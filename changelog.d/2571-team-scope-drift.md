Fixed the Evaluations list showing a coach nothing while the team rating and the player's Evaluations tab showed data for the same players. Team assignment is stored in one table and team *scope* in another; the demo generator and the Excel importer wrote the first without mirroring into the second, so affected coaches held no team scope and the list quietly narrowed to "evaluations I personally authored". A migration backfills the missing scope rows, both import paths now mirror correctly, and deleting a team clears its scope rows instead of leaving them behind.
