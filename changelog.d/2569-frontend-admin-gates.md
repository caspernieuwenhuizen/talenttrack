Closed a permissions hole where any coach could open the Configuration, Custom fields, Evaluation categories, Application KPIs, Lookup normalisation, Roles & rights and Migrations screens by navigating straight to their address, and could call the lookup-normalisation endpoints that change vocabulary academy-wide. Each screen is now gated on the permission that matches what it actually does, so coaches are refused and academy admins are unaffected. Head of Development keeps Evaluation categories and Application KPIs, and no longer reaches the configuration screens — matching the read-only-on-config intent already applied to their dashboard.
