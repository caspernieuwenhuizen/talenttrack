Help topics describe what the product does now, rather than narrating how it
got here. Version stamps, issue numbers and "this used to work differently"
asides are gone from every topic a coach, player or parent can read, and from
most admin topics.

Three topics were also wrong, and are corrected:

- **Impersonation** claimed the audit log could be read over a REST endpoint
  that does not exist, and described a cross-club safeguard that is not
  implemented. It now says plainly that the log is written but has no reader
  surface yet.
- **Custom widgets** told an admin to enable the module with a `wp option`
  command for a setting that no longer exists. It is a toggle on Access
  control → Features.
- **Modules** repeated the same obsolete option name.
