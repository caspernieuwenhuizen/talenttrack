parameters:
    # This baseline is intentionally permissive on first commit.
    # It will be regenerated by CI. Run `composer analyse -- --generate-baseline`
    # locally to refresh it after cleanup passes.
    ignoreErrors:
        -
            message: '#.*#'
            paths:
                - src/Modules/*/*.php
