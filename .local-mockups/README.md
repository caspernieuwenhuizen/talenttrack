# Local mockups

Plain HTML + CSS prototypes for surfaces where a visual design pass before filing the implementation issue pays off. No PHP, no build step, no framework — open the `index.html` directly in a browser.

## Workflow

1. **Pick a surface** to design (typically the high info-density ones — see #947 / #945 conversation history for the shortlist).
2. **Iterate the HTML** in the surface's directory until the layout, density, and interaction reads right at the target viewport.
3. **Test on real device** — for mobile-first surfaces (most of them), open in your phone's browser via dev server or a tunneled URL.
4. **When the design settles, file the implementation issue** with the mockup directory referenced as the spec. The executor's job becomes "port this HTML/CSS to the rendered PHP/template output" — small risk, no design questions to guess at.
5. **Once shipped, the mockup stays** as the design-of-record for that surface so future refinements have a baseline to diff against.

## Why this lives in the repo (not in a Figma / external tool)

- Versioned alongside the code that implements it — diff history matches the implementation history.
- Plain HTML/CSS = anyone can iterate, no tool licenses, no export/import friction.
- The same CSS conventions the production codebase uses (`.tt-*` prefixes, CSS variables, mobile-first) so the port is mechanical.
- Untracked in `.gitignore` is **not** desired — these ARE part of the repo's design-of-record. Commit them.

## Directory convention

```
.local-mockups/
  README.md           ← this file
  <surface-slug>/
    index.html        ← main mockup; multiple states toggled via a state picker
    notes.md          ← optional — open design questions, things still to test on real device
    screenshots/      ← optional — captured states for stakeholders who can't open the HTML
```

## Surfaces currently mocked

- `match-execution/` — live-match sideline view (mobile-first, 360×640 budget). State picker at top toggles between not_started / first_half / half_time / second_half / pending_review / finalized (legacy `finished` still selectable for diffing).
- `match-execution-sections/` — **shell** proposal for the same surface: replaces the one-long-scroll container with a pinned match bar, a state-derived tab strip in the thumb zone, one scrolling panel, and a pinned state CTA. The sections themselves are unchanged — `match-execution/` stays the design-of-record for those. Variant picker toggles **B · tabs in the thumb zone (chosen)**, A · tabs on top, and today's baseline; state picker toggles live / pending review. `notes.md` carries the section inventory per state, the height budget, the two bugs the investigation turned up (#2916, #2917), and the rulings taken on 2026-08-26: variant B, `RecordSpine` grows a non-navigating tab mode (#2932), `config/focus_surfaces.php` suppresses the shell bottom bar on surfaces that own the thumb zone (#2933), a layout toggle shaped like `ShellPreference` (#2934), destructive CTAs arm on the first tap (#2936), no tab badges, and the open tab surviving a reload.
- `match-executions-list/` — dedicated listing surface at `?tt_view=match-executions` PLUS the `MatchesNeedingReviewWidget` hero-widget preview (toggle via the picker). Surfaces the orange "pending review" pills and grey "finalized" pills that the list view + execution view share.
- `wizard-chrome/` — three visual alternatives for the wizard step indicator + action button row, alongside the v3.110.102 baseline for diffing. Toggle via the picker; "Compare all 3" lays the variants side-by-side at ≥1100px. Brief in `notes.md` covers tradeoffs + a recommendation.
- `player-goal-intake/` — **print** mockup (A4 portrait) for the season-start goal-setting 1:1. Two surfaces in one file via the picker: (a) per-player intake form, **3 pages** (snapshot → goals 1+2 → goal 3+afronding), printed before each conversation; (b) methodology reference sheet, **up to 3 pages** with per-section selection (Spelprincipes / Voetbalhandelingen / Leerdoelen), printed once and brought to every meeting. Fully Dutch on print; EN source → NL .po mapping table in `notes.md` for the executor. Vocabulary lifted verbatim from `database/migrations/0018_methodology_full_content.php`.
- `vct-phase2/` + six sibling dirs (`vct-session-wizard/`, `vct-session-coach-view/`, `vct-library/`, `vct-config-tiles/`, `vct-team-panel/`, `vct-phv-flag/`) — **VCT Phase 2 mockup batch** gating implementation children VCT-9 through VCT-14 (#1062). Shared design tokens + intensity bands + exercise categories documented in `vct-phase2/notes.md`. Each surface mockup is responsive (mobile-first 360px), uses the same teal accent + intensity palette, and references the others where cross-surface concerns appear (PHV exclusions surface in both `vct-phv-flag/` and `vct-session-wizard/` step 4).
- `list-toolbar/` — restyle of the shared list filter/search toolbar (`FrontendListTable` / `.tt-list-table-filters`) gating #1753. State picker toggles four directions (**D · Register — recommended**, A · Card, B · Minimal, C · Pill) plus today's baseline for diffing. Mobile-first; `notes.md` carries the recommendation, deck-token mapping, and port acceptance.
- `layout-patterns/` — **five canonical SaaS screen layouts** as monochrome wireframes, for the "how much effort is a visual restructure?" investigation. Not a surface design: no colour, no typography, no components — only where the regions sit. Every pattern is drawn with the same seven labelled zones (A brand … G inspector) at desktop and 360px, with a per-pattern effort estimate grounded in `TileRegistry` / `DashboardShortcode` / the 132 stylesheets. Picker at top; "Compare all" lays the five plus today's shape side by side. `notes.md` carries the effort table and the open decisions — including the CLAUDE.md §5 conflict that gates patterns 1–4.
- `training/` — **10 surface mockups** for the Training module epic (`specs/0096-epic-training-module.md`), gating its nine implementation waves. Hub at `training/index.html`; shared CSS `_shared.css`; notes at `training/notes.md`. Covers the generator wizard, plan builder, sideline run view, merged exercise library, exercise detail with an animated scene player, the `scene_json` diagram editor (a working model of the contract — drag a player, watch the keyframes appear), per-player training exposure, the HoD methodology-coverage matrix, the plan list, and the A4 print. Mobile-first, Dutch copy, 2026 green/gold language.
- `standard-reports/` — **12 report mockups** gating implementation children of #1035 (#1063). 6 CURATED reports as full responsive views (`player/minutes-played/`, `team/minutes-distribution/`, `team/squad-evaluation-summary/`, `season/season-summary/`, `season/trial-funnel/`, `scout/scout-report-card/`) + 6 PRESETS as single-screen explorer-config snapshots (`player/evaluations-received.html`, `player/goal-progress.html`, `team/activity-volume.html`, `activity/evaluation-coverage.html`, `activity/attendance-vs-squad.html`, `season/prospect-logging.html`). Shared CSS `_shared.css` (curated) + `_preset_shell.css` (presets); shared notes at `standard-reports/notes.md`.

- `test-trends/` — **measurement trend surfaces** gating #2536 (player test timeline) and #2537 (Test trends report). Hub at `test-trends/index.html` carries the governing rule: the trend's shape follows the measurement's own `value_type` + `direction`, both of which already exist in the schema (migrations `0175` / `0192`), so no migration is needed. A test with `direction = neutral` (height, weight, shoe size) has no better or worse, so it gets **values in columns per date — no chart, no target band, no ranking**; numeric tests with a direction get a line chart with the target band; `status` gets a coloured step strip / matrix; `passfail` gets ticks plus a pass rate. State pickers on both files toggle the types. Open questions (axis orientation for lower-is-better, how many lines to draw) in `test-trends/notes.md`.

- `minutes-statistics-grid/` — **manual goals and assists** in the minutes grid, which the mockup renames **Minutes + statistics**. Closes the gap where `GoalContributionQuery` (#2859) can only read output for matches that were run on the live match sheet, so a club doing post-match admin has players whose minutes are complete and whose goals are permanently blank. Picker toggles three entry surfaces — **A · sub-columns `Min｜G｜A` with switchable stat columns (chosen)**, B · one box with a focus popover and a `72+2+1` shorthand, C · an expanding per-player row band — plus today's baseline. All three carry a footer row reconciling attributed goals against the recorded scoreline. Settled 2026-08-29; every decision is in `notes.md` and the implementation issue is #3094.

- `match-prep-pdf/` — **print** mockup (A4 landscape) for the match-prep PDF export, which today prints at ~5pt on portrait paper with half the sheet blank. Picker toggles four layouts of the same blocks — **A · Drieluik (chosen 2026-09-02)** — the screen's own order on paper — plus **B · Opstelling boven** (large pitches across the top), **C · Praatplaat** (goals at 9pt, line-up reduced to a reminder), **D · Kwadranten** (equal blocks, no hierarchy), and a faithful simulation of today's output with the wasted area marked. Every variant reserves 15 ruled rows for individual player goals, and in A and C the roles list sits under the selection (same squad, same read) to free a full-height column for them. `Krappe selectie (22)` applies the shrink-to-fit density step to whichever layout is showing. Deep-links via `?v=B&dense=1&ruler=1`. Decisions, the located causes (file + line) and the exporter changes are in `notes.md`.

- `record-link-hover/` — the global `.tt-record-link` hover state (`public.css` 1234–1252), which lifts an inline name 1px and drops a 12px shadow behind it — the card idiom applied to a word, on ~760 links across 27 surfaces. Picker toggles **C2 · getinte pil + rustcue (chosen 2026-09-02)**, A · accent + onderstreping, B · alleen onderstreping, C · pure pil, D · markeerbalk, plus today's baseline and a six-up **Vergelijk alle**. Two checkboxes carry the real questions: *Rusttoestand* suppresses hover to show what a phone sees (today: nothing marks a link as a link), and *48px touch-vloer* applies the `#2800` coarse-pointer floor. Settled 2026-09-02: **C2**, tinted in **info blue** (`--tt-link` = the existing `--tt-info` #2d6fb3), rolled out **globally + tokenised** (`--tt-link` / `--tt-link-hover` / `--tt-link-wash` in `tokens.css`). `notes.md` carries the located cause, the four defects — including `outline: none` on `:focus-visible`, which ships fixed whichever variant wins — and the port acceptance list.
- `activity-completeness/` — the **attendance / minutes completeness readout** on the activities list, gating #3447. Variant picker carries **A · right rail (chosen)**, B · meta chips, C · footer strip, plus today's baseline. A is chosen because the counts align down a single x-position, which is the whole point of a scannable column; B lands them at a different x on every row and C costs ~30px of card height. `notes.md` carries the denominators (planned roster falling back to the team, minutes against players marked Present or Late) and the port acceptance list.
- `activity-weekday-badge/` — the **weekday on an activity's date**, which today shows nowhere on the activities list row or the detail hero even though the dashboard's Upcoming table, the Today bucket header and every planner column already print one. Picker toggles four placements against today's baseline — **A□ · three lines weekday / day / month in a tile held square (chosen 2026-09-16)**, B · weekday joined to the month line, C · weekday inline before the day number, D · weekday in the meta tail. Everything but the weekday element is production CSS copied verbatim from `frontend-activities-manage.css`, and each panel measures its own card heights live, which is what settles it: B and C need a 64–68px grid column to avoid wrapping (24px off the title), and D wraps the meta on long rows only, giving a ragged 70–88px list. A□ is the only variant that stays uniform, and at 52×52 it is 5px shorter per row than the same three lines squeezed into the shipped 44px column. Locale picker (EN/NL/DE/FR) stress-tests abbreviation width. `notes.md` carries the surface list and the open decision.

## Not yet mocked (high-value candidates)

- `player-profile/` — primary working surface, 7-8 tabs. Highest leverage; would resolve recurring "I can't find X" pilot feedback.
- `coach-dashboard/` — widget grid layout, hero priorities.
- `team-detail/` — multi-zone (roster + fixtures + KPIs + planner relationship).

Pattern-locked surfaces (wizards, flat CRUD, list tables, settings) don't need mockups — the framework already governs their shape.
