# Squad evaluation summary — team

Per-player × per-category matrix of mediaan ratings over a window
(default last 12 months). Colour-coded cells (red < 3, neutral 3,
green > 4). Toggle between main categories and sub-categories.

Drill: row click → player profile evaluations tab. Cell click →
explorer with the right fact + grouping.
