# Minutes distribution — team

Squad load balance. Horizontal bar per player sorted by minutes
high → low. Bars in orange below 50% of leader = imbalance flag.
KPI strip surfaces "Spreiding (top vs bottom)" as % delta with a
warn-state when > 30%.

Drill: bar click → per-player minutes-played report.
