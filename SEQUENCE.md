# TalentTrack backlog — status

Per-topic status ordered by what's actionable now, then by what's shaped, then by what's behind it. Updated in every release commit per the DEVOPS.md ship-along rule.

## In progress

_None._

## Ready (shaped, decisions locked)

| # | Topic | Type | Spec | Estimated |
| - | - | - | - | - |
| 0006 | Team planning module — Principles domain + per-session plan-state + calendar UI for week-by-week training planning. Five sprints scoped: (1) Principles schema + plan-state, (2) calendar view, (3) session detail + drilldown, (4) repeat templates, (5) sharing + audit. | epic | [specs/0006-epic-team-planning-module.md](specs/0006-epic-team-planning-module.md) + [specs/0006-sprints-1-to-5.md](specs/0006-sprints-1-to-5.md) | ~55h / ~8-12h compressed |
| 0010 | Multi-language (FR/DE/ES) — regenerate stale `talenttrack.pot` (246 → ~1000+ msgids) then ship FR/DE/ES `.po` skeletons. Likely lower than the original estimate now that #0029 doc split + #0025 auto-translate are in. | feat | [specs/0010-feat-multi-language-fr-de-es.md](specs/0010-feat-multi-language-fr-de-es.md) | ~80-140h v1 |
| 0016 | Photo-to-session capture — coach photographs a paper plan, vision API extracts drills + durations + flow, lands in the new-activity wizard's review step pre-filled. Depends on a vision provider choice (OpenAI / Anthropic / Google). | epic | [specs/0016-epic-photo-to-session-capture.md](specs/0016-epic-photo-to-session-capture.md) | ~80h / ~12-16h compressed |
| 0063 | Export module — single owner of every outbound data artefact (PDF, CSV, Excel, JSON, iCal, GDPR ZIP). 15 use cases, 7 open Qs, depends on PDF-engine + async-runner decisions. Foundation gates 5 v1 use cases + 10 v2; GDPR subject-access (use case 10) gates on Comms #0066. | epic | [specs/0063-epic-export-module.md](specs/0063-epic-export-module.md) | ~105-145h |
| 0065 | TalentTrack Admin Center — TT-side phone-home client shipped in v3.65.0 (see Done). Mothership receiver + dashboard tracked in the separate `talenttrack-admin-center` repo, sub-spec #0001 there; remaining for this repo is the future Remote Actions + Billing Oversight sub-specs that depend on the receiver going live. | epic | [specs/0065-epic-admin-center.md](specs/0065-epic-admin-center.md) | ~95-140h remaining |
| 0066 | Communication module — single owner of every outbound message (push, email, SMS, WhatsApp deep link, in-app notice). Foundation + 5 v1 use cases of 15 total. Enforces #0042 youth-contact rules. Reuses #0063 Export for attachments. 8 open Qs. | epic | [specs/0066-epic-communication-module.md](specs/0066-epic-communication-module.md) | ~90-130h |
| 0078 | Custom widget builder — admin-authored persona-dashboard widgets backed by a registered `CustomDataSource` class layer (no free-text SQL, no visual SQL builder). 5 reference sources (players / evaluations / goals / activities / pdp) ship in v1. 4 chart types: table / kpi / bar / line. Storage in new `tt_custom_widgets` (uuid + tenancy column). REST CRUD + `/data` endpoint. Builder admin page (TalentTrack → Custom widgets) does pick-source → columns → filters → format → preview → save. Persona-dashboard editor palette gains a "Custom widgets" group. New cap `tt_author_custom_widgets` (admin + tt_head_dev); rendered widgets inherit viewer's existing tile/cap gates. Per-widget transient cache (5min default, configurable, manual flush). Audit-log on save / publish / delete. Six phases (~25h+15h+25h+25h+20h+10h). All 10 architecture forks resolved in shaping. Feature-flag-gated default off so beta installs opt in. | epic | [specs/0078-epic-custom-widget-builder.md](specs/0078-epic-custom-widget-builder.md) | ~120h |

## Parked (shaped, awaiting trigger)

| # | Topic | Type | Spec | Trigger to start |
| - | - | - | - | - |
| 0043 | Knowledge Base platform — public-facing `kb.talenttrack.app` SSG site with FlexSearch + REST embed in plugin help drawer | feat | [specs/0043-feat-knowledge-base-platform.md](specs/0043-feat-knowledge-base-platform.md) | `docs/` article count >30, or customer asks for KB search ≥2× per quarter, or marketing wants public KB for SEO |

## Needs refinement / shaping

_None._ Every open idea has been shaped into a spec; the `ideas/` folder holds only its README.

## Not started (no shaping needed before build)

_None._ Every backlog item is now either in **Ready**, **Parked**, or **Done**.

## Blocked

_None._

## Done

| # | Topic | Type | Shipped | Estimated | Actual |
| - | - | - | - | - | - |
| v3.91.2-menu-landing-and-teams-staff | Two regressions, one ship. **(1) Top-level "TalentTrack" still landed on Dashboard layouts** even after v3.90.0's "Account is the new landing" change. WordPress builds the parent menu's `<a>` href from `$submenu[parent][0][2]` once submenus exist; after `removeDashboardMirror()` strips the auto-clone, whichever submenu was registered first wins. `PersonaDashboardModule::boot()` runs before `CoreSurfaceRegistration::register()`, so `tt-dashboard-layouts` was at index 0 and won the click. `Menu::removeDashboardMirror()` now also promotes `tt-account` to position 0 in `$submenu['talenttrack']`, so the click lands on Account regardless of registration order. Idempotent. **(2) Wp-admin Teams list staff columns rendered '—' for every row** because `TeamsPage::render` walked `PeopleRepository::getTeamStaff()`'s result with `array_filter` over a flat list of row objects (`$r->functional_role_key`). Since v3.71.0 that method has returned a *grouped* nested array: `[role_key => [ [ 'person' => $obj, ... ], ... ]]`. Filter matched zero entries on PHP 7 (and would have hard-crashed on PHP 8.x). Fixed: read the group bucket directly and unwrap each entry's `person` object. Multiple head coaches on one team now render comma-separated, matching the v3.88.1 `TeamsRestController::list_teams()` GROUP_CONCAT shape. No new translatable strings — both fixes are code-only. | fix | v3.91.2 | ~30min estimated | ~30min actual |
| v3.91.1-seed-topup-0079 | Hotfix to v3.91.0. The 10 new tile-visibility entities introduced in v3.91.0 (`team_roster_panel` etc.) live in `config/authorization_seed.php` but the seed file is only loaded into `tt_authorization_matrix` on fresh install (migration 0026) or via the admin "Reset to defaults" button. Existing installs that updated to v3.91.0 had the new tile gates pointing at matrix rows that didn't exist — lookup returned false and FR-assigned head coaches stayed locked out of the coach-side dashboard despite migration 0062 having backfilled their `tt_user_role_scopes` rows. Found on the pilot install: head-coach Kevin Raes (linked WP user + FR assignment + scope row all in place per migration 0062) saw only Methodologie + Analytics tiles after v3.91.0. New migration `0063_authorization_seed_topup_0079.php` walks the seed file the same way the v3.39.0 precedent (`0035_authorization_seed_backfill`) did and `INSERT IGNORE`s every (persona, entity, activity, scope_kind) tuple — existing rows including operator-edited ones stay untouched; only the new tuples land. Idempotent. | fix | v3.91.1 | ~30min estimated | ~25min actual |
| 0079 | Tile-entity disambiguation + FR assignment scope sync. Sibling to #0071 — closes the last two gaps where the authorization matrix gave a wrong answer at runtime. (a) 10 new tile-specific matrix entities (`team_roster_panel`, `coach_player_list_panel`, `people_directory_panel`, `evaluations_panel`, `activities_panel`, `goals_panel`, `podium_panel`, `team_chemistry_panel`, `pdp_panel`, `wp_admin_portal`) distinct from the underlying data entities (`team`, `players`, …) which keep their REST + repository role. Seeded for assistant_coach / head_coach / team_manager (`r[team]`), head_of_development / academy_admin (`r[global]`); `wp_admin_portal` is academy-admin-only; scout / parent / player have no grant on any panel. Tile registrations updated; redundant `cap_callback` arguments dropped. New helper `TileRegistry::entityForViewSlug()`. New helper `DashboardShortcode::matrixDispatchAllows()` collapses the per-class role gates (`is_coach \|\| is_admin`, `tt_view_reports`) into one matrix-driven gate; per-class notice strings collapse to one *"You do not have access to this surface."* The `me_slugs` `$player` data prereq stays. Drops seed bug `frontend_admin: r global` for scout. (b) `PeopleRepository::assignToTeam()` and `unassign()` now sync `tt_user_role_scopes` rows so MatrixGate's team-scope check returns true for FR-assigned coaches — fixing the head-coach-only-sees-Methodology bug. Idempotent across multi-role-on-same-team. Backfill migration `0062_fr_assignment_scope_backfill.php` covers existing installs. Docs (EN + NL `access-control.md`) gain two short sections; one new translatable string. | feat | v3.91.0 | ~10-15h estimated | _TBD_ |
| v3.90.2-demo-selective-gen-and-wipe | Operator on a pilot install set up real teams + players + people manually then wanted to wipe only demo activities/evaluations/goals while keeping the real master data. Today's wipe was all-or-nothing; today's generation already had master-data toggles but always wrote dependents on top. Reworked both around the same six-category grid. **Generation**: 3 new dependent-entity checkboxes (`gen_activities` / `gen_evaluations` / `gen_goals`) added to Step 0.5 alongside the existing master-data checkboxes; default ON; compose with hybrid `$skip_*` rules so source rules + operator opt-out both AND in. Master-data flags still procedural-only; dependent flags apply to every source. **Deletion**: single "Wipe demo data" button replaced with a six-checkbox grid (teams / people / players / activities / evaluations / goals); each box renders with cascade-aware row count from new `DemoDataCleaner::categoryCounts()`; default no boxes checked. Each pick expands to its FK-driven cascade per `DemoDataCleaner::CATEGORIES` map (e.g. teams → team_person + activities + attendance + evaluations + eval_ratings); union deduplicated server-side; deletion still walks `DATA_ORDER`. Result notice now reports "wiped N rows across <cats>". `wipeData()` signature changed to `wipeData( ?array $categories = null )` — null falls back to v3.85.0 "wipe everything except people" behaviour for back-compat (only one in-tree caller). Persistent demo WP users still flow through the unchanged separate "Wipe demo users too" form with three safety rails. Renumbered v3.90.1 → v3.90.2 after Excel-upload hardening landed mid-CI. **Deferred**: live JS preview of cascade-total update on checkbox change; per-batch wipe (`batch_id`-scoped). | feat | v3.90.2 | ~3-4h estimated | ~2h actual |
| v3.90.1-demo-excel-upload-hardening | User reported a Demo Data Excel upload error that "almost looks like a hosting server side error" — no TalentTrack red-notice bounce, just a generic page. Three fixes for the three failure modes that produce that symptom. **(1) `\Throwable` catch instead of `\Exception`** in `ExcelImporter::importFile()`. PhpSpreadsheet surfaces `\TypeError` on malformed XLSX, and an OOM during `load()` throws `\Error` / `\OutOfMemoryError`. Either bubbled out as a fatal that the host's reverse proxy turned into a generic 500. Now caught and turned into the existing friendly "Could not read the workbook: …" red bounce. **(2) Memory + time limits raised** before invoking the importer — `wp_raise_memory_limit('admin')` + `set_time_limit(0)` in `handleExcelImport`, plus `wp_raise_memory_limit('admin')` in `handleGenerate` when `source !== 'procedural'`. PhpSpreadsheet wants 64-128MB even with `setReadDataOnly(true)` and shared hosts default below that. The importer call is also wrapped in another `\Throwable` catch as defence-in-depth; a fatal slipping past the inner catch now logs class + message + filename and bounces. **(3) `post_max_size` overflow detection** at the top of both upload-receiving handlers (`tt_demo_excel_import` and `tt_demo_generate`). When PHP discards `$_POST` + `$_FILES` because the request body exceeded `post_max_size`, `admin-post.php` would otherwise refuse the action entirely (no `action`, no nonce, generic "Are you sure you want to do this?" page). New `postMaxSizeExceeded()` helper checks for `REQUEST_METHOD=POST` + non-zero Content-Length + empty `$_POST` and bounces with the actual `post_max_size` value. **Plus**: `UPLOAD_ERR_*` codes now get specific operator-readable messages instead of "Upload failed (error code 1)" — `UPLOAD_ERR_INI_SIZE` names `upload_max_filesize`, `UPLOAD_ERR_NO_TMP_DIR` names `upload_tmp_dir`, etc., via new `uploadErrorMessage()` helper. The upload form surfaces the server's actual limits below the file input (`upload_max_filesize` / `post_max_size` / `memory_limit`) so operators can size their workbook before it fails. 14 new NL msgids. Renumbered v3.89.2 → v3.89.4 → v3.90.1 across three parallel-agent collisions (v3.89.2-player-delete + v3.89.3-migration-0061 + v3.90.0-admin-menu-reorg all landed mid-rebase). | fix | v3.90.1 | ~30-45min estimated | ~30min actual |
| v3.90.0-admin-menu-reorg | Three-part wp-admin menu cleanup. **(1) Account becomes the TalentTrack landing.** Top-level "TalentTrack" click now lands on `AccountPage::render` instead of the stats-and-tiles dashboard; the legacy dashboard view is reachable via a new "Dashboard" submenu (slug `tt-dashboard`, callback `Menu::renderDashboardTiles`). Top-level cap stays `read` so non-operators can land safely; AccountPage opens with the Plan tab for them. **(2) PlanOverviewPage folded into Account as a tab.** AccountPage gains nav-tab dispatch with two tabs: **Account** (operator-only, `tt_edit_settings` — tier display, trial/grace banner, usage vs caps, trial CTAs, phone-home diagnostics) and **Plan & restrictions** (everyone with `read` — caps table with at-cap colouring + 3-column Free/Standard/Pro feature matrix with effective tier highlighted). `?tab=plan|account` controls dispatch; default is Account for operators, Plan for non-operators. Standalone `Admin\PlanOverviewPage` class deleted; `LicenseModule::boot` no longer initialises it; `tt-license-plan` slug retired. **(3) Spond / Migrations / Help & Docs relocated.** Spond moved from direct `add_submenu_page` (priority 30) into the Configuration group via `AdminMenuRegistry`; Migrations moved from `MenuExtension` direct registration (priority 20) into Configuration group too (cap `tt_view_migrations`; pending-count badge dropped — pending-migration banner remains the primary visibility surface); Help & Docs gets its own "Help" group separator instead of trailing the Access Control items invisibly. New `M_SPOND` constant in `CoreSurfaceRegistration`. Account submenu cap relaxed from `tt_edit_settings` → `read` because the page now hosts the read-only Plan tab. Trial-handler redirects updated to land on `&tab=account`. 1 new NL msgid. | feat | v3.90.0 | ~3h estimated | ~1.5h actual |
| v3.89.3-migration-0061-backfill-deleted-players | Companion to v3.89.2 — repairs rows that prior versions had mangled. Migration `0061_backfill_player_status_deleted_to_archived_at` runs on plugin update: `UPDATE tt_players SET archived_at = updated_at, status = 'active' WHERE status = 'deleted' AND archived_at IS NULL`. `archived_at` ← `updated_at` (closer to the actual delete time than NOW()); `status` ← `'active'` (so unarchive later doesn't strand the row in a status nothing reads); `archived_by` left NULL. Idempotent — WHERE clause skips already-correct rows. **Why a migration vs. manual SQL**: operators shouldn't copy-paste SQL to recover from plugin bugs. Migrations run on every install that updates, get tracked in `tt_migrations`, and don't depend on the operator escaping prefixes or picking club_id. | fix | v3.89.3 | ~30min estimated | ~25min actual |
| v3.89.2-player-delete-archived-at | Operator on a pilot install reported clicking **Delete** on a frontend player row did nothing visible. Root cause: `PlayersRestController::delete_player()` (and `PlayersPage::handleDelete()` in wp-admin) wrote `status='deleted'`, but the list query filters on `p.archived_at IS NULL` — DELETE wrote to a column the list didn't read. `status` is a lifecycle marker (active/trial/inactive); `archived_at` is the soft-delete flag (mirrors `delete_team`). Both paths now write `archived_at = current_time('mysql')` + `archived_by = get_current_user_id()`. Confirm-dialog text "They will be archived" now matches what actually happens. Active-roster counts (TeamStatsService, ActivitiesRestController roster_size, Menu badges, ReportsPage) were already silently dropping deleted-status players via their own `status='active'` filter — those numbers are unchanged. Existing `status='deleted'` rows from prior versions stay visible in the list; one-time backfill UPDATE optional (out of scope). | fix | v3.89.2 | ~30min estimated | ~30min actual |
| v3.89.1-scrub-pilot-name | Editorial sweep — no runtime behaviour change. Replaced specific pilot-customer name ("JG4IT") and identifying subdomain URL across public repo artifacts with neutral phrasing ("pilot install", "pilot client theme", etc.) so the public changelog / docs / source comments don't disclose who's piloting the product. Files touched: `CHANGES.md`, `readme.txt`, `SEQUENCE.md`, `docs/configuration-branding.md` + nl_NL counterpart, `specs/shipped/0023-…`, `specs/shipped/0064-…`, `assets/css/frontend-admin.css`, plus inline comments in `LicenseGate.php`, `AccountPage.php`, `WizardEntryPoint.php`, `TeamsRestController.php`, `ActivitiesRestController.php`, `QueryHelpers.php`. **Untouched**: `marketing/pitch/pitch-deck-nl-pilot-meeting.md` (the deck is addressed *to* the pilot, content is internally consistent — flag for the user to move out of the public tree if concerned); git commit messages (immutable history). Memory rule saved: customer-identifying names go in private notes, not public-repo artifacts. GH release notes for prior versions also edited via `gh release edit`. | chore | v3.89.1 | ~30min estimated | ~30min actual |
| v3.89.0-user-permission-compare | Follow-up to the pilot install six-item operator batch (item 5; items 1+2+6 in v3.88.1, items 3+4 verified working). New admin page **TalentTrack → Authorization → Compare users**: two `wp_dropdown_users` pickers + diff-only / show-all toggle. Per-user header card with WP roles + resolved TT personas via `PersonaResolver::personasFor()`. 5-column comparison table (Capability / Matrix entity:activity / User A state / User B state / Diff dot) walking `LegacyCapMapper::knownCaps()`; per cap, computes legacy state from `$wp_user->allcaps` and matrix state via `MatrixGate::canAnyScope()`. Effective = legacy OR matrix; rows where effectives differ are yellow-highlighted. Diff-only is default (headline use case: "why does coach A see this tile but coach B doesn't"). Dormant-matrix banner when `tt_authorization_active = 0` so operators understand the matrix column is computed-but-not-runtime. GET-form so URL is bookmarkable. Read-only — no admin_post handlers, no destructive actions. Cap gate `current_user_can('administrator')`. **Deferred**: N-user (>2) comparison, CSV export, per-cap drill-down to which matrix scope row grants/denies — add if asked. ~10 new NL strings (mostly UI labels). | feat | v3.89.0 | ~3-4h estimated | ~1.5h actual |
| v3.88.1-team-head-coach-and-list-cleanup | Three pilot install fixes from the same six-item operator batch as v3.86.2 (items 1, 2, 6 of that report; items 3+4 verified working on the live install; item 5 — compare two users' permissions — ships next as separate feature). **Fix 1 (Teams list head-coach empty)**: column read the legacy `tt_teams.head_coach_id` wp-user pointer; operator assigns HCs via Functional Roles which writes `tt_team_people` × `tt_functional_roles.role_key='head_coach'`. `TeamsRestController::list_teams()` + `get_team()` now derive HCs via two `GROUP_CONCAT` sub-selects; multiple HCs render comma-separated as clickable links to each person's detail. Legacy `LEFT JOIN wp_users ON head_coach_id` retained as fallback. Tenancy filters on every join (tp.club_id = t.club_id, p.club_id = tp.club_id, fr.club_id = tp.club_id). **Fix 2 (functional-roles dropdown English-only)**: `FrontendFunctionalRolesView::renderAssignForm()` echoed `$r->label` raw; wrapped with `LabelTranslator::functionalRoleLabel()` (existed since v3.82.0; two other render sites in same file already used it). One-line fix. **Fix 3 (attendance % shown as 0% for planned/cancelled)**: `ActivitiesRestController::format_row()` computed pct regardless of `activity_status_key`; operators on the pilot install mistook 0% for "no-one showed up" instead of "didn't happen yet". Status guard added — null (renders blank) when status in [planned, cancelled]. No new NL strings (reuses existing translator + lookup labels). Renumbered v3.87.1 → v3.88.1 after v3.88.0 (matrix-controlled-tiles list cleanup) landed mid-CI. | fix | v3.88.1 | ~2h estimated | ~1.5h actual |
| v3.88.0-matrix-truly-single-source | User follow-up on v3.87.0: matrix admin page still listed tiles under "Tiles not controlled by the matrix" even though most were declared with `entity` and ARE matrix-controlled. Two fixes. (1) `MatrixEntityCatalog::callbackGatedTiles()` shipped in v3.86.0 before the `entity` field existed and never checked for it — list logic updated: a tile counts as matrix-controlled when EITHER it declares `entity` OR its `cap` maps to an entity in `LegacyCapMapper::tupleFor()`. Anything that fails both rungs lands on the list. (2) "Open wp-admin" was the only frontend tile the v3.87 sweep missed — now declares `entity = 'frontend_admin'`. **Net result on a stock install**: the list is empty, every tile is matrix-controlled. The list itself stays as forward-compatibility insurance — a future module that registers a tile without an entity OR a matrix-mapped cap will appear here so the operator notices the regression. | fix | v3.88.0 | ~30min estimated | ~20min actual |
| v3.87.0-matrix-single-source-of-truth | User reported: Scout could see the Migrations tile even though R/C/D was greyed out for `migrations` in the matrix. Same wiring class as v3.86.0 audit-log fix, generalised. **Refactored the gating model**: when a tile declares a matrix `entity` AND `tt_authorization_active = 1`, the matrix is the SOLE source of truth for tile visibility — cap + cap_callback become fallbacks. WP administrators continue to bypass via existing MatrixGate / LegacyCapMapper admin shortcut. **Swept 30 tile registrations** in `CoreSurfaceRegistration::registerFrontendTiles()` to declare matrix entities (player-self / tasks / people / performance / development / reference / trials / analytics / dev-ideas / admin / staff-dev clusters). Migrations admin menu in `Shared\Admin\MenuExtension::register()` also moved from `tt_view_settings` to `tt_view_migrations`. **Matrix admin grouping** now derives from the tile registry: `MatrixEntityCatalog::groupForEntity()` walks all registered tiles and returns the (translated) group label of whichever tile consumes the entity, so matrix rows cluster under the same groups + same locale as the operator sees on the frontend dashboard. Module-class fallback still applies for back-office-only entities (`authorization_changelog`, `impersonation_log`) that no tile consumes — those land in "Other" at the bottom. Group order respects tile-registration order, so matrix reads top-to-bottom in the same shape the dashboard scrolls through. **What's still callback-gated**: tiles can declare both `entity` and `cap_callback`; entity gate runs first under matrix-active, callback fires for tiles without entity. Welcome wizard / License account / demo-data tools intentionally have no matrix entity (no seed row). | feat | v3.87.0 | ~3-4h estimated | ~2h actual |
| v3.86.2-wizard-url-and-lookup-translation | Two pilot install bugs, both with structural framing per operator request. **Bug 1 (wizard URL 404)**: `?tt_view=wizard&slug=new-team` (and the resume-banner's `…&dismiss_resume=1` variant) 404'd on installs where the dashboard isn't the WP home page. `WizardEntryPoint::dashboardBaseUrl()` was building URLs off `$_SERVER['REQUEST_URI']` which resolves to `/` for query-only URLs — `[talenttrack_dashboard]` shortcode never fired. Same root cause family as the v3.85.0 RecordLink fix on a different code path. Fix: delegate to `RecordLink::dashboardUrl()` (configured `dashboard_page_id` → shortcode-discovery scan → REQUEST_URI → home_url chain) then strip wizard query args. Defensive REQUEST_URI fallback retained. **Bug 2 (preferred-foot dropdown English-only)**: four render sites (`PlayersPage`, `FrontendPlayersManageView` filter + edit form, `RosterDetailsStep` wizard) each built the dropdown ad-hoc — three queried `tt_lookups` raw without `LookupTranslator`, the wizard had a hardcoded `['', 'left', 'right', 'both']` array with `ucfirst()` for labels (English-only and ignored custom lookup values). Structural fix: new `QueryHelpers::get_lookup_label_pairs(string $type): array` returning `[stored_name => translated_label]` pairs via the render-aware translator. All four sites now consume that one helper; future lookup dropdowns get translation + custom-value support in one line. 1 new NL msgid (`— not specified —`). Renumbered v3.85.5 → v3.86.1 → v3.86.2 across two successive collisions: v3.86.0 (auth matrix discoverability) landed on first push, v3.86.1 (license enforcement bundle) landed on the second. | fix | v3.86.2 | ~1h estimated | ~45min actual |
| v3.86.1-license-enforcement-bundle | User-driven audit + fix of every action that should be license-gated, shipped as PRs 1+2+3 bundled into one ship to avoid merge-train pain. **PR 1**: shared `LicenseGate::allows()` / `enforceFeatureRest()` / `enforceCapRest()` helpers so REST + wizard + admin paths use one decision layer; REST `POST /players` + `POST /teams` return 402 with `license_cap_*` codes when at cap; new-player + new-team wizard `ReviewStep` cap-check pre-insert; `PlayerCsvImporter` cap-checks per-row so each blocked row carries the upgrade message; frontend Players + Teams manage views hide the New button + render `UpgradeNudge::capHit` when at cap. **PR 2**: gate Pro/Standard features that had no enforcement — Trials (5 frontend views + REST controller) hard cut to UpgradeNudge / 402 when `trial_module` not allowed; TeamChemistryView gated on `team_chemistry`; ScoutHistoryView + ScoutMyPlayersView gated on `scout_access`; FunctionalRolesView gated on `functional_roles`. Existing data stays in the DB. **PR 3**: read-only `Plan & restrictions` admin page (everyone with the read cap, not just operators) — current tier + trial/grace banner, caps table with at-cap warning, 3-column Free/Standard/Pro features matrix with operator's effective tier highlighted. Distinct from operator-only `AccountPage` which handles Freemius checkout. **Deferred**: `radar_charts` / `undo_bulk` / `partial_restore` / `s3_backup` declared in FeatureMap but no code reads the gate yet — runtime free for all, PlanOverview matrix shows future restriction. ~25 new NL strings. (Original PR shipped without CHANGES.md / readme.txt / SEQUENCE.md updates; backfilled in v3.86.2.) | feat + fix | v3.86.1 | ~6-8h estimated | unknown |
| v3.86.0-auth-matrix-discoverability | User reported: clearing R/C/D for HoD on the `audit_log` entity in the matrix didn't hide the Audit log tile. Root cause: tile gated on the umbrella `tt_view_settings` rather than the granular `tt_view_audit_log`, so the matrix bridge revoked the granular cap but the tile asked the broader one. Same shape across 7 other settings-area surfaces (Custom Fields / Eval Categories / Category Weights / Seasons / Functional Roles in admin menu + admin dashboard). All eight switched to their specific sub-cap so each maps 1:1 to a matrix entity. **Matrix admin page** gains: localized entity labels (slug + module class shown as caption underneath), bidirectional "Used by: <tile-list>" labels under each entity row (walks `TileRegistry::allRegistered()` + `AdminMenuRegistry::allEntries()` + `AdminMenuRegistry::allDashboardTiles()` and resolves their `cap` → entity via `LegacyCapMapper::tupleFor()`), fuzzy search input matching across slug + label + module + consumer name (typing "podium" jumps to the entity behind the Podium tile), sticky `<thead>` + sticky first column, dormant-bridge warning banner when `tt_authorization_active = 0`, and a "Tiles not controlled by the matrix" section listing the 17 callback-gated tiles that bypass the matrix entirely (follow-up). New helpers: `LegacyCapMapper::capsForEntity()`, `TileRegistry::allRegistered()`, `AdminMenuRegistry::allEntries()` + `::allDashboardTiles()`, new `MatrixEntityCatalog` class. ~10 new NL strings (most entity labels reuse existing msgids). **Deferred:** the 17 cap_callback tiles (Podium, parent dashboards, etc.) need each callback inspected before refactor — separate PR. Per-tile "what gates this?" admin affordance also held until the callback refactor lands. | feat + fix | v3.86.0 | ~3-4h estimated | ~2h actual |
| v3.85.0-demo-selective-and-link-fix | Two user-driven changes. **Demo generator selective generation**: new fieldset on Tools → TalentTrack Demo Data with three checkboxes — Generate teams / Generate people + WP users / Generate players (default all ON). Unchecking any tells the generator to use existing master data in the club instead of creating new rows. Validation fails fast if a category is opted out but DB has 0 rows of that entity. New helpers `DemoGenerator::loadAllTeams()` / `loadAllPlayers()` (no demo-tag filter); `firstAdministratorId()` fallback for `created_by` when UserGenerator is skipped. Designed for the "I set up real teams + people + players manually, fill in activities on top" workflow. **Dashboard URL fix**: `RecordLink::dashboardUrl()` was producing `/talenttrack/?tt_view=…` on subdomain installs where the dashboard page is also the site's home page. Two bugs: (1) `discoverDashboardPageId()` scanned for `[tt_dashboard` but the actual shortcode is `[talenttrack_dashboard]` — corrected search token + `has_shortcode()` call. (2) `get_permalink()` returned the page slug regardless of `page_on_front` — new `permalinkOrHome()` helper returns `home_url('/')` when the page is the front page. Mirrored on `FrontendAccessControl::dashboardUrl()`. ~9 new NL strings. | feat + fix | v3.85.0 | ~3h estimated | ~1.5h actual |
| v3.84.0-i18n-bundles-8+9 | i18n audit (May 2026) Bundles 8 + 9 — final closeout. **Bundle 8** (JS error-fallback sweep): three live `\|\| 'Error'` fallbacks in `admin-reorder.js`, `frontend-list-table.js`, `functional-roles.js` now route through `TT.i18n.error_generic` (already in the `DashboardShortcode` localize bundle) before falling back. The audit's other ~10 `cfg.foo \|\| 'English'` patterns were verified dead code on localized sites. **Bundle 9** (methodology task arrays research): re-verified `0018_methodology_full_content.php` task arrays — all already structured as `[ 'nl' => [...], 'en' => [...] ]` MultilingualField; audit was a false positive, no code change. **Audit progress: 9/10 bundles shipped, ~140 of ~150 surfaces fixed.** May 2026 i18n audit functionally closed. Bundle 10 (letter templates DE/FR/ES/IT/PT) stays deferred to multi-language epic #0010. Schema-change vision items deferred from Bundles 4+5 (translations columns on tt_roles / tt_functional_roles / tt_eval_categories) wait for SaaS multi-locale requirement. | fix | v3.84.0 | ~1h estimated | ~30min actual |
| v3.83.0-i18n-bundles-5+6 | i18n audit (May 2026) Bundles 5 + 6. **Bundle 5** — five eval-category render sites bypassing `EvalCategoriesRepository::displayLabel()` (the translator that runs each label through `__()`). Wired through: `Wizards\Evaluation\HybridDeepRateStep`, `Wizards\Evaluation\RateActorsStep`, `FrontendEvalCategoriesView` (edit header + list link), `FrontendReportDetailView` (comparison table column headers). Seeded labels (4 mains + 21 subcategories) already in the .po, so wiring change alone fixes Dutch rendering. Schema change for admin-created custom labels deferred. **Bundle 6** — `TemplateBuilder.php` README sheet content (~32 lines of demo-data import instructions) was raw English literals never wrapped in `__()`. Wrapped each line. Sheet identifier tokens (Sessions, Session_Attendance, Teams, etc.) inside the prose stay English literals because the importer reads sheet names exactly. The actual sheet rename to "Activities" is deferred (separate breaking change). ~25 new NL msgids. Audit progress: 7/10 bundles shipped, ~125 of ~150 surfaces fixed. | fix | v3.83.0 | ~3h estimated | ~1h actual |
| v3.82.1-post-0077-followups | Hotfix bundle for v3.80.0 (#0077) follow-up complaints. **#1 Guest-add error message generic.** JS read `resp.body.message` but the RestResponse error envelope puts the human message at `errors[0].message`; every failure surfaced "Could not add guest." regardless of the actual cause. Now reads errors[0]; falls back to body.message then to the generic. **#3 Impersonation had no UI.** The service + banner + admin-post handler shipped in #0071 child 5; nothing actually invoked them. New `Authorization\Admin\ImpersonationPage` admin submenu under TalentTrack with wp_dropdown_users + reason field; end-impersonation continues via the existing banner. **#4 Persona dashboard links → root.** `RecordLink::dashboardUrl` fell back to `home_url('/')` when `tt_config.dashboard_page_id` was unset/zero. Now self-heals — scans for a published page hosting `[tt_dashboard]` and caches the discovered id back into config. Last-resort uses current REQUEST_URI sans tt_view params. **#6 KPI cards weren't clickable.** New `AbstractKpiDataSource::linkView()` returns sensible defaults per KPI id (goal_completion_pct → goals, my_evaluations_received → my-evaluations, …); `KpiCardWidget` wraps the body in `<a>` when set; `.tt-pd-kpi-link` CSS keeps typography intact + adds 1px hover-lift. **#9 Link underline not editable.** New `link_underline` token (KIND_SELECT, options always/hover/never). CSS has no native "hover only" decoration value, so VisualEditor emits explicit rest + :hover blocks via `renderLinkUnderlineOverrides` on `.tt-link, .tt-record-link, .tt-login-links a`. **#2 HoD persona dashboard too narrow.** Curated 8-tile set expanded to 18 tiles in 4 logical bands (work / master-data / methodology+planning / reports+tooling / governance) — HoD holds caps on every tile listed. **#8 Drag-and-drop dumped at end.** `placeNewSlot` and `moveExistingSlot` received the drop event but never read `clientX/Y`; every drop landed at (0, max_y+1) so DnD was visually identical to a palette click. New `gridCellFromEvent` translates pointer coords → (col, row) clamped to the 12-col canvas. ~15 new NL strings. Renumbered through v3.80.1 → v3.81.2 → v3.82.1 to escape successive parallel-agent collisions on v3.80.1 (i18n Bundles 1+2), v3.81.1 (Custom CSS button-underline) and v3.82.0 (i18n Bundles 3+4+7). | fix | v3.82.1 | ~3-4h estimated | ~2.5h actual |
| v3.82.0-i18n-bundles-3+4+7 | i18n audit (May 2026) Bundles 3 + 4 + 7 — stored content Dutch backfill (same pattern across three different stores). **Bundle 3** — Migration `0060_seed_lookup_translations_nl` writes `meta.translations.nl_NL.name` (and `description` where applicable) for ~30 foundational `tt_lookups` rows that shipped English-only since `0001_initial_schema`: foot_option (3), age_group (Senior), goal_status (5), goal_priority (3), attendance_status (5), eval_type (3 names + 3 descriptions), cert_type (6 from 0048), behaviour_rating_label descriptions (5 from 0042), potential_band descriptions (5 from 0042). Idempotent — only writes when meta has no translations.nl_NL.name/description yet. **Bundle 4** — `LabelTranslator::authRoleLabel(?string)` covers the 9 `tt_roles` system labels; `LabelTranslator::functionalRoleLabel(?string)` covers the 6 `tt_functional_roles` system labels. Both return `null` for unknown keys so callers fall back to the row's typed `label` for custom roles. Wired into `FrontendPeopleManageView`, `FrontendFunctionalRolesView`, `FrontendTeamsManageView`. **Bundle 7** — `LabelTranslator::trialTrackName()` (Standard/Scout/Goalkeeper) + `LabelTranslator::formationName()` (Neutral/Possession/Counter/Press-heavy 4-3-3). Wired into FrontendTrialsManageView/FrontendTrialCaseView/FrontendTrialParentMeetingView/FrontendTrialTracksEditorView, the parent-letter `{track_name}` substitution in `LetterTemplateEngine` (the audit-flagged "deelgenomen aan onze Goalkeeper-stage" → "Keeper-stage" bug), and the formation REST endpoints in `TeamDevelopmentRestController`. 6 new NL msgids. Audit progress: 5/10 bundles shipped, ~95 of ~150 surfaces fixed. | fix | v3.82.0 | ~5h estimated | ~2h actual |
| v3.81.0-i18n-bundles-1+2 | i18n audit (May 2026) Bundles 1 + 2. **Bundle 1** — 5 critical-path bugs that ship raw English to Dutch users today: (a) inline JS in `FrontendConfigurationView.php` lines 394/396/448 routed through new localized `T_ERROR`/`T_NETWORK_ERROR` constants; (b) `csv-import.js` status badges (Error/Dupe/OK) + responsive `data-label` headers (Row/Status/Player/DOB/Team/Notes) + 3 error-message fallbacks now read from 13 new `TT.i18n.csv_*` keys in `DashboardShortcode.php`; (c) `admin-methodology-media-picker.js` had hardcoded English `att.title \|\| 'Image'` fallback AND hardcoded Dutch `'Wordt toegevoegd bij opslaan'` (English-locale users saw Dutch) — both fixed via `TT_MethodologyMedia.imageAlt`/`pendingLabel`, msgids switched to English so .po controls translation; (d) hardcoded `aria-label="Remove"` on `multitag.js` + `aria-label="Dismiss"` on `flash.js` now read from `TT.i18n.remove`/`dismiss`; (e) three wizard steps rendering raw `tt_lookups.name` values via `echo esc_html($n)` now route through `LookupTranslator::name()` / `byTypeAndName()` / `LabelTranslator::attendanceStatus()` — `Wizards\Evaluation\AttendanceStep`, `Wizards\Evaluation\HybridDeepRateStep`, `Wizards\Activity\DetailsStep`. **Bundle 2** — ~30 user-visible "session"/"sessions" strings inside `__()` swept to "activity"/"activities" so the Dutch translations actually match. Surfaces: `ActivitiesRestController` (8 REST error toasts), `RolesPage` (4) + `FunctionalRolesPage` (2) + `Activator` defaults (5 stored-text rows for `head_coach`/`manager`/`physio`/`team_member`/eval_type Training), `CoreSurfaceRegistration` (3 tile descriptions), `PlayerReportRenderer` (3 + section name in `AudienceDefaults`), `CoachDashboardView` (3) + `CoachForms` (2) + `public.js` JS i18n key rename `save_session`→`save_activity`, `FrontendRolesView` (5 role descriptions), and 13 other surfaces (FrontendMyActivitiesView, FrontendTrialCaseView, PlayerDashboardView, FrontendMyProfileView, MethodologyPage, FormSlugContract, TranslationsConfigTab, FeatureToggleService, BackupPresetRegistry, PlayerStatusCalculator, ModulesPage, Workflow MyTasks, OnboardingPage). Migration `0059_session_to_activity_stored_text` backfills the seeded English text in `tt_lookups.description` (eval_type Training row) + `tt_roles.description` (physio + team_member) for existing installs — idempotent, skips admin-edited rows. CI gate `legacy-sessions-gate` (#0035) gained a new step that scans PHP quoted strings for user-visible "session" vocabulary with allow-list of legitimate uses (browser sessions, possession, log keys, capability codes, internal section keys) — the pre-existing step only caught identifier-level leaks. ~30 new NL msgids; orphaned "session"-bearing msgids stay (harmless). Audit progress: 2/10 bundles shipped, ~80 of ~150 surfaces fixed. Renumbered from v3.79.x → v3.80.0 → v3.81.0 to escape successive parallel-agent collisions on v3.79.0 (#0075 Sprint 3 close) and v3.80.0 (#0077 polish wave 1). | fix | v3.81.0 | ~6h (Bundles 1+2) | ~3h actual |
| 0075 | Full design system — expanded v3.64 visual editor (#0064) from 21 flat fields to **88 tokens / 18 categories** across Sprint 0 (PRG hotfix + tab-class + font dropdowns), Sprint 1 (5 PRs: token catalogue + accordion editor + consumer wiring + status notice classes + live preview JS + type-scale + Buttons + Forms), Sprint 2 (2 PRs: typography consumer wiring + h4-h6 + Links + 6 new categories Content/Cards/Lists/Tables/Feedback/Overlays + per-state buttons + REST endpoint + admin bypass fix), Sprint 3 (this PR — wired the deferred consumers). Frontend surface only; wp-admin keeps the v3.64 editor. **Bug fix shipped along the way**: `LegacyCapMapper::evaluate()` had no WP administrator bypass — admin without explicit academy_admin functional role got denied for every `tt_*` cap that flowed through `user_has_cap` when matrix is active. Fixed in #185. The catalogue is the single source of truth; both server-side `VisualEditor::generateCss` and the browser-side live preview JS in `FrontendCustomCssView::renderLivePreviewScript` walk it. REST exposure via `GET /wp-json/talenttrack/v1/design-system/tokens`. Storage shape stays flat blob (handles 88 tokens fine; structured `{tokens, components}` migration parked for a real consumer). Templates rebuild + remaining tertiary/ghost/icon button variants + media/dashboard/utility/accessibility tokens deferred to follow-up issues if asked. | epic | v3.78.1 + v3.79.0 (Sprint 3 close) | ~140h epic + ~3-4h hotfix | ~12h actual (Sprint 0+1+2+3 across two sessions) |
| 0077 | Polish wave 1 — foundations + 11-module consistency pass shipped as a single squash-merge per user directive (avoids the merge-train rebase pain that would otherwise cascade across 14 dependent surfaces). **Foundations**: F1 reusable HelpDrawer component (Components\HelpDrawer::button + docs-drawer.js per-trigger topic override + wizard help icon button); F2 FrontendViewBase::breadcrumbs() opt-in chain + FrontendBreadcrumbs component, retrofitted on player + report detail views; F4 WP_DEBUG-only ModuleCompletenessReport admin page scoring 8 entity modules across 7 facets (list/detail/edit/widget/kpi/docs en+nl). **Eleven module changes**: M1 persona dashboard catalogue dropdown (Widget::dataSourceCatalogue() + per-widget overrides for ActionCard/InfoCard/MiniPlayerList/DataTable/NavigationTile, replacing free-text data_source input with a closed-set picker); M2 frontend Activities principles linkage + admin Guests read-only parity + public.js formToJSON name[] array fix; M3 frontend Goals linked_principle/linked_action selects + GoalsByPrincipleKpi (90d coverage %) + GoalsByPrincipleSource DataTable preset; M4 trial cases 6-tab → linear-with-anchor-nav UX + trial players surfaced on team detail page; M5 teams cap audit — frontend + REST upgraded to userCanOrMatrix, role→cap loop confirmed re-asserted on every runMigrations(); M6 frontend comparison Chart.js radar + trend overlay parity (renderChartScripts mirrors admin); M7 functional-roles assignment_count audit (documented the future-fold seam since no tt_club_people exists today) + 0-assignment +Assign empty-state CTA; M8 player detail → six-tab case page (Profile/Goals/Evaluations/Activities/PDP/Trials) with linkified evaluations; M9 PDP cycle polish — derived per-conversation status enum (no migration: scheduled/held/signed_off from existing dates), '(X of N signed off)' progress indicator, parent/player ack icons, drawer help; M10 player-created goals approval flow — migration 0058 adds Pending Approval lookup row, REST forces pending_approval on tt_player creator, approval gated to head coach via new AuthorizationService::isHeadCoachOfPlayer (org policy decision: granting tt_player the cap is operator-side); M11 native frontend reports for team_ratings + coach_activity (replaces target=_blank wp-admin jumps) + browser-native window.print() PDF (no Dompdf dependency added) + tile harmonisation. Renumbered from a v3.79.0 candidate to v3.80.0 to escape the v3.79.1 #0075 follow-ups landing on main mid-CI. ~50 new NL strings; per-module docs in EN + NL. | epic | v3.80.0 | ~140h epic estimate | two sessions: foundations + Sprint A+B then Sprint C+D + polish + ship |
| v3.78.0-deferred-polish | Deferred-polish bundle from #0072 + #0073. **#0073 follow-up**: `TableRowSourceRegistry` backfill — `trials_needing_decision` (open/extended trial cases joined with players + teams), `recent_scout_reports` (`ScoutReportsRepository::listForGenerator()` with revoked/expired/active status), `audit_log_recent` (`AuditService::recent()`). Three new sources in `src/Modules/PersonaDashboard/TableSources/`. **#0072 follow-ups**: (a) Autosave indicator. New `POST /talenttrack/v1/wizards/{slug}/draft` REST endpoint (`WizardDraftRestController`) — sanitises field map, merges into `WizardState` which writes through to `tt_wizard_drafts`. New `assets/js/wizard-autosave.js` — `input`+`change`-listening, 800ms-debounced, handles checkboxes/radios + `key[]` arrays, skips identical-payload sends. New status caption with `aria-live="polite"` cycling Idle / Saving… / Saved · HH:MM / Failed. (b) Resume banner. `FrontendWizardView::renderResumeBanner()` checks `WizardState::hasPersistentDraft()` + `persistentDraftAge()` > 10min + state has user-typed payload, shows "You started this %s ago. Continue or start over?" notice. (c) Per-row Review submit. Extracted `EvaluationInserter::insert()` from `ReviewStep::submitActivityFirst()` so the new `POST /wizards/new-evaluation/insert-row` endpoint and the PHP submit share one code path. New `assets/js/wizard-eval-review.js` intercepts Review-step Submit click, drives one POST per rated player with `<progress>` bar showing "Writing evaluation N of M…", redirects on done. JS-disabled fallback unchanged. 10 new NL strings. Mobile-vs-desktop responsive split for `RateActorsStep` deferred to its own ship (significant CSS + new swipe UI). | feat | v3.78.0 | ~6-9h estimated (mobile split deferred) | ~2.5h actual |
| v3.77.3-migration-0054-fix | Migration 0054 (HoD persona narrowing, #0071 child 3) failed with `Unknown column 'actor_user_id' in 'INSERT INTO'`. The audit-log insert in the migration's `up()` used four column names that never existed on `tt_audit_log`: `actor_user_id` / `object_type` / `object_id` / `meta_json`. The actual schema (created in migration 0002, payload-renamed in 0030, tenancy column added in 0038) uses `user_id` / `entity_type` / `entity_id` / `payload`. The cap-stripping itself worked — only the audit-trail write failed — but the migration runner aborted the whole migration as a result so the cap state was inconsistent across HoD users (some stripped, some not, depending on user-id ordering inside the foreach). Fixed: corrected the four column names. The migration is idempotent (the cap-removal loop short-circuits on `has_cap` already-false), so re-running on a partial install completes cleanly without writing duplicate audit rows. | fix | v3.77.3 | ~10min | ~10min |
| v3.77.1-demo-autotag | Auto-tag records created while demo mode is ON. Follow-up to issue #26's demo-mode discussion: "treat all data created during demo=on as demo data". `apply_demo_scope()` filters reads to rows tagged in `tt_demo_tags`, but only the bulk demo generator was tagging on insert. Records created by an operator while demonstrating the product silently disappeared on save — `loadSession`-style fetches returned null even though the row was in the table (same root cause as the #13 Spond visibility bug). New static helper `DemoMode::tagIfActive( $entity_type, $entity_id, $batch_id = 'user-created' )` checks effective mode + the tag table's existence + idempotency (skip if already tagged) and inserts a `tt_demo_tags` row. Wired into 11 create paths across the 6 demo-scoped entities: REST controllers (Players, Teams, Activities, Evaluations, Goals, People via `PeopleRepository::create`) + admin pages (PlayersPage, TeamsPage, ActivitiesPage, GoalsPage, EvaluationsPage) + the team-setup wizard's auto-create-person path. Refactored ActivitiesRestController's pre-existing inline tag check to use the helper so every site shares one idempotent path. Records created with demo mode OFF are unaffected (the helper short-circuits). | feat | v3.77.1 | ~3h estimated | ~1h actual |
| 0073 + 0074 | HoD landing enhancements + persona dashboard visual refresh — bundled because they touch the same module. **#0073**: new `team_overview_grid` widget (per-team summary cards on the HoD landing, expandable to a per-player breakdown via the new `GET /persona-dashboard/team-breakdown` REST endpoint; slot config `days=N,limit=M,sort=alphabetical|rating_desc|attendance_desc|concern_first`; expand state persisted in `localStorage` per `tt_pd_team_card_{team_id}`); new `upcoming_activities` `DataTableWidget` preset (forward-looking schedule, default 14 days, columns Team / Type / Date & time / Location); new `TableRowSourceRegistry` pluggable contract for `DataTableWidget` row wiring (back-compat empty-state for the three pre-existing presets); new `new_trial` action key on `ActionCardWidget` (view `trials`, cap `tt_manage_trials`); `CoreTemplates::headOfDevelopment()` re-laid: KPI strip → team overview grid + new-trial → upcoming activities → trials needing decision → navigation tiles. New classes `TeamOverviewRepository` + `TeamSummary` + `UpcomingActivitiesSource` + `TeamBreakdownController`. **#0074**: subtle page header at the top of `.tt-pd-landing` (persona name `<h1>` + date/club subtitle, time-of-day greeting prefix for hero-less personas — HoD/Academy Admin/Scout — suppressed on sub-views); decorative `tt-pd-tile-icon` + `tt-pd-action-icon` removed (tiles → typographic hierarchy + hover chevron `tt-pd-tile-arrow`; action labels → "+" prefix in the translated string); `assets/css/persona-dashboard.css` rewritten with `:root { --tt-pd-* }` token block, refreshed widget shells (14px corner radius, two-stop shadow + inset hairline, hover-lift), refined typography (panel titles 0.9375rem semibold sentence-case; KPI numerals tabular-nums + 700 weight), 1.5rem desktop band gap. ~16 new NL strings (action labels with `+`, page-header greetings, team-overview labels, AJAX status messages). Renumbered from a v3.76.0 candidate to v3.77.0 to escape a tight version-number race with the parallel agent's #0075 Sprint 1 PR 5 shipment which claimed v3.76.0 mid-CI. | feat | v3.77.0 | ~12-18h + ~6-9h estimated | ~3.5h actual |
| v3.76.1-issue-23 + 0076-spec | Two-fer (renumbered v3.75.3 → v3.76.1 to escape parallel-agent v3.76.0 ship). (1) Issue #23 — buttons showing URLs as labels. Root cause: `FrontendBackButton::render( string $label = '' )` ignored the URL parameter callers were passing — every site that wanted a custom target URL got the URL rendered as the button text instead, with the link still navigating to the dashboard. 10 callers across the codebase were doubly broken. Fixed the signature to `render( string $target_url = '', string $label = '' )` so passing-a-URL does what it always meant; the single caller that passed a label as first arg (`FrontendUsageStatsDetailsView`) updated to the new shape. Added explicit "← Back to players" / "← Back to teams" / "← Back to people" labels on the three master-data detail views the user screenshotted. ~3 new NL strings. (2) Spec #0076 — Playwright coverage expansion epic. Locks the 8 per-flow specs (players-crud, teams-crud, evaluation, activity, goal, pdp-capture, persona-dashboard-editor, lookups-frontend) + sequencing + DoD so the v3.75.2 foundation has a clear next-step plan when the user is ready to expand. Trigger to start: foundation runs cleanly on 5+ PRs first. | fix + spec | v3.76.1 | ~1h estimated | ~1h actual |
| v3.75.1-issue-12 | Automated UI testing — Playwright foundation. Ships the skeleton + one smoke test (admin login → TalentTrack dashboard) so the foundation can be validated before the multi-week investment in per-flow coverage. New top-level files: `package.json` (Playwright + `@wordpress/env` devDeps + `npm run` scripts), `playwright.config.js` (Chromium-only, single worker, traces-on-retry, HTML reporter), `.wp-env.json` (Docker-based WP test instance on :8889 with TT plugin auto-mounted from working tree), `.gitignore` (excludes `node_modules`, `playwright-report`, `test-results`). New tests directory: `tests/e2e/login.spec.js` (the smoke test) + `tests/e2e/README.md` (local quick-start, CI overview, roadmap). New CI: `.github/workflows/e2e.yml` runs Playwright on every PR + push to main, uploads HTML report + test-results as artifacts on failure. v1 trade-offs: Chromium only (Firefox/WebKit deferred until smoke flows are stable), single worker (parallel deferred until tests are isolated), `admin / password` shared account via wp-env's defaults (programmatic auth helper deferred). Roadmap captured in the README for the per-flow expansion: player / team CRUD, evaluation create + rate, activity create + attendance, goal lifecycle, PDP capture, persona dashboard editor, lookups CRUD. Bumped to v3.75.1 to recover from a parallel-agent collision on v3.75.0 (which shipped the new evaluation wizard rebuild for #0072). | feat | v3.75.1 | foundation: ~4h estimated, full coverage: multi-week | foundation ~1h actual |
| 0072 | New Evaluation wizard rebuild — replaces the two-step pre-flight (PlayerStep + TypeStep → flat form) with a six-step activity-first / player-first graph: `ActivityPickerStep` (smart-default landing; `notApplicableFor()` skip when no recent rateable activities) → `AttendanceStep` (writes real `tt_attendance` rows; auto-skipped when already recorded) → `RateActorsStep` (one row per `meta.quick_rate`-flagged category per present/late player, deep-rate accordion, Skip checkbox) → `ReviewStep` (final submit creating N evaluations with `activity_id` set), with `PlayerPickerStep` → `HybridDeepRateStep` → `ReviewStep` as the player-first ad-hoc fallback. Migration `0057_new_evaluation_wizard` adds `tt_eval_categories.meta TEXT` + `tt_evaluations.activity_id BIGINT` (the column the spec assumed but didn't exist), seeds `meta.rateable=false` on `clinic`/`methodology`/`team_meeting`, seeds `meta.quick_rate=true` on the four conventional categories (Technical/Tactical/Physical/Mental matched by name), creates the new `tt_wizard_drafts` table for cross-device draft persistence (multi-tenant via `club_id`, unique on `(user_id, wizard_slug)`). `WizardState::save/load/clear` extended with table-backed write-through so transient expiry doesn't lose drafts. Daily `tt_wizard_drafts_cleanup_cron` (filterable TTL `tt_wizard_draft_ttl_days`, default 14d). `QueryHelpers::isActivityTypeRateable()` + `isCategoryQuickRate()` read helpers (default true / false respectively). Configuration → Lookups admin gains a Rateable checkbox per activity-type row, merging into existing `meta` JSON. Wizard slug stays `new-evaluation`; cap stays `tt_edit_evaluations`; old PlayerStep + TypeStep deleted. Trivial bundled fix: `WizardEntryPoint::dashboardBaseUrl()` docblock said `[talenttrack_dashboard]`; actual shortcode is `[tt_dashboard]`. Deferred to follow-ups: per-row progress at Review submit, Locked/Editable badges with 24h countdown, autosave indicator + REST endpoint, mobile-vs-desktop responsive layout split, resume banner UI (the persistent store + `hasPersistentDraft()` helper ship; only the banner UI is missing). Closes #0072 functionally; polish items tracked separately. | feat | v3.75.0 | ~25-35h estimated | ~3.5h actual |
| v3.74.4-issue-7 | Auto-translate on lookup save with admin preview. New REST endpoint `POST /translations/preview` that takes `{ text, source_lang }` and returns translated values for every other installed WP locale via the configured `TranslationLayer` engine (DeepL or Google). The frontend lookup editor (#5 surface) gains a "Translate to other languages" button + per-locale editable inputs (`translations[<locale>]`). On click, the button POSTs the source name, fills the empty per-locale inputs with engine output, and surfaces a "Translated. Review and edit before saving." message — the admin can correct anything before persisting. On save, non-empty per-locale values are persisted into `meta.translations.<locale>.name` so `LookupTranslator::name()` picks them up. Engine + API key are configured under Configuration → Translations (existing surface from #0025); when `TranslationLayer::isEnabled()` returns false the preview endpoint surfaces a clear error and the inputs stay empty (admin can still type translations manually). Source-language defaults to the first 2 chars of `get_locale()` so an `nl_NL` site translates into `en_US` etc. Confirm option: option C from shaping (auto on save with admin preview before persist). Engine: DeepL when API key is configured. | feat | v3.74.4 | ~4h estimated | ~1.5h actual |
| v3.74.3-issue-5 | Lookup admin moved to the frontend. The Lookups sub-tile now lists every lookup category with an icon; clicking opens an inline CRUD editor at `?config_sub=lookups&category=<slug>` instead of jumping to wp-admin. Each category gets a dedicated editor: list of current rows (sort_order, name, optional description, optional colour swatch, locked indicator), Edit / Delete row actions, and an Add / Edit form below with `name` + `sort_order` + optional `description` (eval_type / activity_type) + optional `meta.color` colour picker (activity_type / goal_status / attendance_status). Save / delete go through the existing `LookupsRestController` PUT / POST / DELETE endpoints — authorisation, tenancy and audit logging stay centralised. New `lookupCategoryMeta()` registry maps the 9 frontend-editable lookup slugs (`eval_types`, `activity_types`, `game_subtypes`, `positions`, `foot_options`, `age_groups`, `goal_statuses`, `goal_priorities`, `att_statuses`) to their `tt_lookups.lookup_type` plus per-category presentation flags. Rating-scale stays special-cased on its existing `?config_sub=rating` form because it lives in `tt_config`, not `tt_lookups`. Drag-reorder intentionally deferred to v2 — `sort_order` is editable as a numeric field; the user didn't answer the v1 question. | feat | v3.74.3 | ~12h estimated | ~2h actual |
| v3.74.2-issues-14-15-17 | Behaviour-vs-potential split + tie to activity + recorded date. The data-layer split was already done in #0057 Sprint 1 (`tt_player_behaviour_ratings` with `related_activity_id` + `rated_at`, separate `tt_player_potential` with `set_at` + `potential_band`, separate caps `tt_rate_player_behaviour` / `tt_set_player_potential`). The UI was missing the activity tie-in and showed `created_at` (insert timestamp) instead of `rated_at`/`set_at` (the meaningful when-it-happened date). `FrontendPlayerStatusCaptureView` updates: (1) behaviour rating dropdown now uses the configured `rating_min` / `rating_max` / `rating_step` instead of hardcoded 1–5, so clubs that customise the scale see their values; (2) optional "Related activity" dropdown on the behaviour form populated by `loadRecentActivitiesForPlayer()` — distinct activities the player attended with `status='completed'`, last 20; (3) recent-ratings list shows `rated_at` (falls back to `created_at` for legacy rows) plus a "View activity" link when `related_activity_id` is set, opening the frontend activity detail; (4) potential history label updated to "Recorded on" + uses `set_at`. No schema change — repository already accepted `related_activity_id` parameter; the form just wasn't sending it. | feat | v3.74.2 | ~2h estimated | ~1h actual |
| v3.74.1-bundle | Round-3 shaping bundle 1 — four small UX fixes from the deferred shaping list. (1) **#2 PDP staff icon**: the staff PDP tile registered `'icon' => 'pdp'` but no `pdp.svg` exists in `assets/icons/`, so `IconRenderer::render()` returned an empty string and the tile rendered iconless. Switched to `'methodology'` (exists, fits the staff-development theme, distinct from regular PDP's `'goals'`). (2) **#1 CSV import location**: bulk Players CSV import was reachable only from a button on the Players page. Added a "Players CSV import" tile to the Frontend Configuration grid (cap-gated on `tt_edit_players`, surfaced as the primary entry); the Players-page button stays as a secondary entry for power users already on that surface. (3) **#8 Configuration tile icons**: every Configuration sub-tile + admin-bound tile gained an icon column rendered through the existing `IconRenderer` SVG set (no new assets — reused dashboard / rate-card / settings / categories / weights / gear / import / sessions / migrations / docs / audit-log / lightbulb), plus a small `.tt-cfg-tile-icon` style block. Mirrors the wp-admin dashboard tile look. (4) **#13 Spond rows invisible in demo mode**: `apply_demo_scope()` filters out non-demo-tagged rows in DEMO_ON, including real Spond-imported activities. The team page bypasses the scope so it leaked Spond events through, but clicking one landed on "Activity not found" because the activities list/detail DID apply the scope. Activity entity now exempts `activity_source_key = 'spond'` from the filter — Spond data is operational, not part of the demo set. | feat | v3.74.1 | ~3h estimated | ~1.5h actual |
| v3.73.0 (#0071 follow-up) | Closes the two follow-ups deferred from v3.72.0's #0071 ship. (1) **Impersonation guards** — new `ImpersonationContext::blockDestructiveAdminHandler()` wrapper wired into 23 destructive admin handlers (matrix Apply/Rollback/Save/Reset, role grants/revokes, backup restore + bulk-undo, demo generate/wipe-data/wipe-users/excel-import, migration run-one + run-all, season save + set-current, all 6 `tt_delete_*` handlers — activity/lookup/evaluation/goal/player/team — and BulkActionsHelper). While impersonating, attempting any of these returns 403 with the standard "switch back to perform this operation" message. (2) **Sub-cap refactor** on the explicit Settings-adjacent surfaces — `ConfigurationPage` (save_toggles → `tt_edit_feature_toggles`; save_lookup + delete_lookup → `tt_edit_lookups`), `MigrationsPage` (render → `tt_view_migrations`; runners → `tt_edit_migrations`), `SeasonsPage` (render + handleSave + handleSetCurrent → `tt_view_seasons` / `tt_edit_seasons`), `TranslationsConfigTab::handleSave` + `Translations\CapThresholdNotice::render` → `tt_edit_translations`, `CustomFieldsRestController` (5 routes) → `tt_view_custom_fields` / `tt_edit_custom_fields`, `EvalCategoriesRestController` (5 routes) → `tt_view_evaluation_categories` / `tt_edit_evaluation_categories`. CapabilityAliases roll-ups keep existing `tt_edit_settings` holders working transparently — no user loses access. Still deferred: ConfigRestController (generic key-touching endpoint) + the "is admin proxy" sub-uses of `tt_edit_settings` in PdpFilesRestController, ThreadsRestController, etc. (separate cleanup, ~10 sites). Bumped from the v3.72.x patch series to v3.73.0 to escape a tight version-number race with parallel-agent shipments. | feat | v3.73.0 | n/a | ~1.5h actual |
| v3.72.4-issue-31 | Phone-home diagnostic surface (renumbered from v3.72.3 → v3.72.4 to recover from a parallel-agent collision on v3.72.3). A pilot install was not phoning home while another install on the same operator's network was — same code base, different runtime behaviour. Without runtime access to the silent install, can't root-cause specifically, but added the data + tooling so the operator can self-diagnose on any install. New "Admin Center phone-home" block on the wp-admin Account page shows: `install_id`, configured endpoint, last-sent timestamp + HTTP code, last-phoned plugin version, next daily cron schedule (or a red warning when wp-cron is not registered). New "Send now" button fires `Sender::sendDiagnostic()` synchronously and surfaces the receiver's response inline (HTTP code, error message, duration, body size, endpoint hit). `Sender::send()` rewritten as a thin wrapper around the new diagnostic variant; both `update_option` writes (`tt_admin_center_last_sent_at` + `_code`) feed the status block. Most likely failure modes the surface helps identify: `DISABLE_WP_CRON` set in wp-config.php with no system cron substitute (next-cron field shows red), outbound HTTPS / DNS blocked (Send now returns WP_Error), receiver actually rejecting the install (4xx code surfaces). | fix | v3.72.3 | ~45min | ~45min |
| v3.72.2-issue-24 | Workflow tasks were never generated when a coach saved a game via the frontend. Root cause: `ActivitiesRestController::create_session()` and `update_session()` did not fire the `tt_activity_completed` action; only the wp-admin `ActivitiesPage::handle_save()` did. The `EventDispatcher` listens on that action and dispatches the post-game evaluation template, which fans out one task per active player to the team's head coach. Frontend saves silently skipped the dispatch. User reported "I completed a game for a team assigned to coach2 but no task appears" — symptom of every coach who edits via the frontend. Added the `do_action` to both REST endpoints, mirroring the admin path's `(TaskContext, activity_type_key)` payload. Also flagged: the event fires unconditionally on every save (planning a future game also fires it), which is pre-existing behaviour from the admin side; the template's own `expandTrigger()` filters by `activity_type` lookup metadata so non-game saves short-circuit. Tightening the firing condition to `status === 'completed'` is a separate behaviour change worth shaping before applying. | fix | v3.72.2 | ~30min | ~30min |
| 0071 | Authorization matrix completeness, sub-cap split, HoD redefinition (sequel to #0033). Five children shipped in one bundle. **Coverage**: `config/authorization_seed.php` rewritten to match the canonical Excel matrix (`docs/authorization-matrix-extended.xlsx`) — ~107 entities including sensitive `player_injuries` / `safeguarding_notes` / `player_potential` / `pdp_evidence_packet` rows, full Trials/StaffDevelopment/Threads/Push/Spond/PersonaDashboard/CustomCss/Translations sub-entities. **Sub-caps**: 12 `tt_view_*`/`tt_edit_*` cap pairs (lookups/branding/feature_toggles/audit_log/translations/custom_fields/eval_categories/category_weights/rating_scale/migrations/seasons/setup_wizard) replace the over-coarse `tt_*_settings` umbrella; new `tt_manage_authorization` cap for the auth-management write surface; umbrella caps remain as `CapabilityAliases` roll-ups (you "have" `tt_edit_settings` iff you hold all 12 sub-caps); migration `0053_settings_subcaps_seed` backfills onto existing umbrella holders. **HoD narrowing**: drops 15 grants from the seed (bulk_import removed; reports/workflow_templates/team_chemistry/spond/documentation/settings/lookups/branding/feature_toggles/rating_scale/persona_templates/translations/custom_field_values dropped to R-only; dev_ideas dropped to C from RCD); HoD keeps full RCD on player-development surfaces; migration `0054_hod_persona_narrowing` strips deprecated edit caps from existing users and writes the diff to `tt_audit_log`; opt-out via `define( 'TT_HOD_KEEP_LEGACY_CAPS', true )`. **Player-status visibility toggle**: new `player_status_visible_to_player_parent` feature toggle (default false on fresh, true for upgrades via migration `0055_player_status_visibility_default`); `PlayerStatusVisibility::dotVisibleTo()` runtime override at REST + render layers; matrix stays the intent, toggle is club policy. **User impersonation**: new `Modules/Authorization/Impersonation/` (Service+Context+Banner+Cron+AdminPost), new `tt_impersonate_users` cap (admin + tt_club_admin only), `tt_impersonation_log` table (migration `0056_impersonation_log`) with actor/target/club_id/started_at/ended_at/end_reason/ip/user_agent/reason, signed `tt_impersonator_id` cookie + `wp_set_auth_cookie` swap, non-dismissible yellow banner via `wp_body_open` + `admin_notices`, daily `tt_impersonation_cleanup_cron` closes orphans >24h, defence-in-depth (no admin-on-admin / no self / no stacking / no cross-club without `tt_super_admin`), `ImpersonationContext::denyIfImpersonating()` guard ready for destructive admin handlers (sweep across handler call-sites deferred). `LegacyCapMapper::MAPPING` extended with 25 new bridges (12 sub-cap view + 11 sub-cap edit + audit_log read-only + tt_manage_authorization + 18 round-2 caps for threads/spond/journey/player-status/pdp-evidence/methodology-config/functional-roles + impersonation cap). EN+NL `docs/impersonation.md` operator guide. ~13 new NL .po strings. Closes #0071. | epic | v3.72.0 | ~70-100h estimated | ~5h actual |
| v3.71.5-issue-11 | Persona dashboard editor drag-drop showed "not allowed" cursor and refused to drop on the canvas. Palette items were rendered as `<button draggable="true">`. Firefox (and some Chromium configurations) don't reliably fire `dragstart` on form-element buttons; without that event firing, `window.__ttPdeDrag` stays null, the canvas's `dragover` handler bails early without `preventDefault`, the browser shows the not-allowed cursor, and the drop can't complete. Switched to `<div role="button" tabindex="0">` for both the widget palette and the KPI palette. Behaves identically for keyboard / screen-reader users (role=button + tabindex=0 + the existing keydown handler that handles Enter/Space) but is reliably draggable across browsers. | fix | v3.71.5 | ~20min | ~20min |
| v3.71.4-issue-26 | Guest player save likely-cause hotfix. Schema + JS + REST controller code paths look correct on inspection; the most plausible failure mode is the same one #19 hit — a coach assigned via Functional Roles passes the matrix scope-row check but `current_user_can('tt_edit_activities')` returns false when the `user_has_cap` matrix bridge is dormant (`tt_authorization_active = 0`), so `ActivitiesRestController::can_edit()` rejects the request with 403 and the JS surfaces "Could not add guest". Extracted the cap-or-matrix fallback (introduced inline in `TileRegistry::userMayAccess` for #19 / v3.71.0) into a central `AuthorizationService::userCanOrMatrix()` helper. Wired into `ActivitiesRestController::can_view()` + `can_edit()`; the prior tile helper now delegates to it so navigation + REST gates share one implementation. Hypothesis-driven fix — if the bug persists after deployment, the cause is elsewhere (likely runtime: form data shape, schema drift, or 2FA/session issue) and needs hands-on debugging. | fix | v3.71.4 | ~30min | ~30min |
| v3.71.3-issue-16 | Notes / English placeholder sweep. Audited every `placeholder=` attribute across PHP + JS for user-visible English text not wrapped in `__()`. The vast majority were already wrapped or are clearly format hints / examples (`+31612345678`, `https://…`, `head_coach`, `2-1`, `2025/2026`, `value\|Label`, `RESTORE`) — those don't need translation. JS placeholders all read from `wp_localize_script`-bound i18n bundles. Only `includes/Admin/Reports.php` had genuine untranslated user copy: `placeholder="Preset name"`, `Save Current Filters` button, and a `Delete` link were inline English. Wrapped all three in `__()` / `esc_html_e()` / `esc_attr_e()`; added Dutch translations. The user's specific "Notes" placeholder report did not surface a hardcoded site — every `Notes` label / placeholder is already wrapped + translated; the visible English likely came from a localised cache or the WP locale not resolving to `nl_NL`. | fix | v3.71.3 | ~30min | ~30min |
| v3.71.2-issue-27 | Goal `on_hold` Dutch label inconsistency. The `tt_lookups` table seeds `goal_status` rows as `Pending` / `In Progress` / `Completed` / `On Hold` / `Cancelled` (Title Case With Spaces), but the `tt_goals.status` column stores `pending` / `in_progress` / `completed` / `on_hold` / `cancelled` (snake_case). `LookupPill::resolveRow()` did an exact name match, missed every row, and silently fell back to the raw stored value — `on_hold` rendered as the literal `on_hold` instead of "In de wacht". `LabelTranslator::goalStatus()` correctly returned the .po-translated string, so the user saw the translation in some places and the raw key in others. Added a `normaliseName()` secondary index that collapses snake_case ↔ Title Case so `on_hold` matches `On Hold`. Fix is generic — applies to every lookup type and any future case-/separator-mismatched values without per-type code changes. | fix | v3.71.2 | ~15min | ~15min |
| v3.71.1-issue-10 | wp-admin sidebar TalentTrack mirror submenu — first submenu under the TalentTrack top-level was the WP-injected clone of the parent (same slug, same callback). Previous treatment renamed it to "Dashboard", which left two visually identical entry points: clicking either landed on the same dashboard. The user kept asking "why is the top item there". `Menu::removeDashboardMirror()` now calls `remove_submenu_page('talenttrack', 'talenttrack')` at admin_menu priority 999, so the only TT dashboard entry point is the top-level "TalentTrack" item itself. | fix | v3.71.1 | ~10min | ~10min |
| v3.71.0-bundle | Round-3 follow-up bundle — five concrete fixes from the user's master list, plus deferral notes for items requiring shaping or runtime debug. (1) **Team detail page staff section was empty** even though staff was assigned. `PeopleRepository::getTeamStaff()` returns rows grouped by functional-role key (`['head_coach' => [{person, ...}]]`); the previous `renderStaff()` loop iterated the outer array as if it were a flat list of staff objects, so `$s->person_id` was always undefined and silent empty `<li>`s emitted. Rewrote to flatten the grouped array, pull the `person` object from each entry, and translate the role key via `LabelTranslator::roleType()`. (2) **`coach2@mediamaniacs.nl` couldn't see player / team tiles** despite being assigned via Functional Roles. Tiles gate visibility through `user_can()`, but the `user_has_cap` filter that bridges legacy `tt_*` caps to `MatrixGate` is dormant when `tt_authorization_active` is 0. New `TileRegistry::userMayAccess()` falls back to `LegacyCapMapper::evaluate()` directly when `user_can` returns false — if the matrix grants the cap on any scope, the tile renders. Runtime gates at the actual write/read sites still scope-check on the entity level; this change only affects the navigation surface. (3) **Goal save didn't return to the goals page**. `FrontendGoalsManageView::renderForm()` adds `data-redirect-after-save="list"` so `assets/js/public.js`'s existing redirect-after-save handler drops the `action`/`id` params and lands the user back on the list. (4) **Activity Source column was never displayed** even though the `activity_source` lookup (manual / spond / generated) was seeded by migration 0040 and the value lives on every row. `ActivitiesRestController::fmtRow` adds `activity_source_pill_html`; both `FrontendActivitiesManageView` and the wp-admin `ActivitiesPage` table show a Source column with a coloured `LookupPill`. Edits flow through the existing Lookups admin so clubs can rename / re-colour without code changes. (5) **i18n**: added `Upcoming activities` Dutch translation (was wrapped in `__()` but not in the .po) plus 14 stub strings for the v3.71.0 surface additions and pending follow-up surfaces (Source / Behaviour / Potential / Recorded on / etc.). **Deferred with explicit reasons**: guest-player save (Bug D — code-path looks correct, needs runtime debugging on a real install), persona-dashboard drag-drop (Bug I — JS race condition in the drag state machine), goal `on_hold` Dutch label inconsistency (Bug E — likely a lookup-translation gap, needs which surface displays untranslated investigated), notes placeholder English (Bug G — find each hardcoded `placeholder=` site), wp-admin top menu broken link (Bug H — `Menu::renameDashboardMirror` can rename a slug that no longer points anywhere), workflow tasks not generated (#24 — deep workflow-engine investigation), a pilot install not phoning home (#31 — instance-specific debug, can't fix from the codebase alone), CSV import location move (#1 — UX choice between Configuration tile vs. inline-on-Players), config icons sweep (#8 — cosmetic surface; touches every Configuration tile), persona toggle discoverability (#9 — design choice between dashboard notice / new tile / wizard prompt). **Deferred awaiting shaping**: lookup admin in frontend (#5 — substantial UX work), auto-translate when entering new lookup value (#7 — translation-engine integration scope unclear), Spond persistence + visibility surface (#13 — architecture: imported activities are already persisted as `tt_activities` rows per #0062, so the user complaint may be about display gating; needs clarification), behaviour-vs-potential split + tie to evaluation (#14/#15 — schema change requires spec), potential capture date (#17 — small but goes with the split), automated UI testing (#12 — Playwright vs. Cypress decision, CI integration plan, in-scope test surfaces). | feat | v3.71.0 | ~6-9h estimated | ~3h actual |
| v3.70.2-hotfix | Round-3 audit hotfix bundle for v3.70.0 + a few older regressions. (1) **Master-data links pointed at site root** — `RecordLink::detailUrlFor()` and every `home_url('/')`-based tt_view URL builder switched to a new `RecordLink::dashboardUrl()` helper that resolves the page hosting the `[talenttrack_dashboard]` shortcode (via `tt_config:dashboard_page_id` → `get_permalink()`), so admins on installs where the dashboard is not at the site root land on the right page. (2) **Activity / goal detail pages didn't exist** — `FrontendActivitiesManageView` + `FrontendGoalsManageView` gain a read-only detail rendering when `?tt_view=<slug>&id=N` arrives without `action=edit`; row-action Edit links now carry `action=edit` so the form is still reachable. `loadSession` extended to join team name. REST controllers (Activities + Goals) repointed `title_link_html` / admin-page Title cells / PDP detail goal links from the player-only `my-activities` / `my-goals` slugs to the generic `activities` / `goals` slugs so academy admins / HoD don't hit the player-self gate (root cause of "as academy admin I get 'only visible for people connected to a player' from team page"). `FrontendTeamDetailView::renderUpcomingActivities` switched to the same generic slug. (3) **Wizard Cancel looked invisible + dumped to dashboard** — class swapped from `tt-button-link` to `tt-button-secondary` so it renders as a real button; cancel destination prefers `?return_to=` query arg → HTTP referrer → dashboard, so cancelling lands back where the user came from. (4) **Demo data admin page disappeared** — registration moved from DemoDataPage's own admin_menu callback into `CoreSurfaceRegistration` via `AdminMenuRegistry` so it survives top-level menu renames. Account page registration confirmed already declarative; if still hidden, the License module is disabled in `tt_modules`. ~0 new strings. | fix | v3.70.2 | ~1-2h estimated | ~1.5h actual |
| v3.70.1-bundle (Logger hotfix) | Saving any setting through the Configuration REST endpoint returned HTTP 500 on PHP 8 — `ConfigRestController::save_config()` ended with `Logger::info(...)` static-called against an instance-method, fatal under PHP 8 (silently deprecated under 7.4). The setting was actually persisted (the log line ran after the write), so the UX showed "Error" while `tt_config` was correctly updated. Fix converts the five public Logger methods (`debug` / `info` / `warning` / `error` / `log`) to `public static`, holds `EnvironmentService` in a static field, and keeps the DI constructor so `Kernel`'s container-bound logger and `AuditService`'s `$this->logger->warning(...)` keep working unchanged (PHP allows calling static methods via `$obj->method(...)`). Unblocks ~68 pre-existing static `Logger::error(...)` / `Logger::warning(...)` call sites scattered across the REST controllers + `EventEmitter` that would have fatal-ed in failure branches. Smoke-test confirms both call styles produce identical formatted output. | fix | v3.70.1 | n/a | ~30min actual |
| 0070 | List linkification follow-up — round-2 audit revealed v3.66.0 (#0067) only linkified the activities + goals frontend lists; players / teams / people / evaluations frontend lists were never wired through `RecordLink` and several admin lists had gaps. One bundled PR closes the long tail. **REST controllers** gain pre-rendered `*_link_html` fields so the generic `FrontendListTable` can show clickable cells via `render: html`: `PlayersRestController::fmtRow` adds `name_link_html` / `team_link_html` / `parent_link_html` (list query gains a `tt_people` join for parent name); `PeopleRestController::fmtRow` adds `name_link_html` / `email_link_html` (email points at `?tt_view=mail-compose&person_id=N` so sends are audited, not raw `mailto:`); `TeamsRestController::fmtRow` adds `name_link_html` / `coach_link_html` (list query joins `tt_people` on `wp_user_id` so the coach name resolves to a person detail). **Frontend manage views** updated to use `'render' => 'html'` for the new fields: `FrontendPlayersManageView` (adds new Parent column), `FrontendPeopleManageView`, `FrontendTeamsManageView`. **Frontend evaluations** (`FrontendEvaluationsView` — flat-render, not `FrontendListTable`) wraps player / team / coach cells in `RecordLink`; query gains the same `tt_people` join. **PDP detail page** wraps connected-goal titles in `RecordLink` to goal detail. **Admin pages** fix the long tail: `PlayersPage` adds Parent column with link; `ActivitiesPage` reorders Title to the second column, wraps Title in `RecordLink` to `?tt_view=my-activities&id=N`, and switches the Team link from the wp-admin edit form to the frontend team detail; `GoalsPage` wraps the Goal title in `RecordLink` to goal detail and switches the Player link to the frontend player detail; `EvaluationsPage` adds a Team column with `RecordLink` to team detail. ~1 new NL string ("Parent" column header). Closes #0070. | feat | v3.70.0 | ~3-5h estimated | ~2h actual |
| 0062 | Spond JSON-API fetcher — replace the never-actually-published iCal fetcher (#0031 was scaffolded against a contract that didn't exist) with the internal `api.spond.com` JSON client. `SpondClient` rewritten as JSON HTTP client (login → 12h-cached `loginToken` → 401-retry-once → group-scoped `/sponds/?groupId=…`); `SpondParser` rewritten as a thin normaliser onto the array shape `SpondSync` already consumed (so the upsert + soft-archive loop is unchanged). 2FA hard-fail in v1 with a recognisable error message. Per-club credentials (one Spond email + password) replace per-team iCal URLs; password encrypted at rest via `CredentialEncryption` (same envelope as `Push/VapidKeyManager`'s VAPID private key); token cache lives in `tt_config` and rotates with `wp_salt('auth')`. Per-team `tt_teams.spond_group_id` column (migration 0052, indexed) replaces `spond_ical_url` (nulled out + kept in schema for one release for rollback safety). Admin UI: `Configuration → Spond` is now the single home — credential form at the top with **Test connection** + **Disconnect**, per-team table below with picked group name + last sync + status + **Refresh now**. Team edit form gets a Spond-group dropdown populated live from `/groups/`; falls back to "Connect Spond first" link when no credentials. New-team wizard gains an optional **Pick Spond group** step (`notApplicableFor()` auto-skips when no credentials). Hourly WP-Cron, WP-CLI (`wp tt spond sync`), per-team REST sync endpoint all unchanged — only the data source moved. EN + NL `docs/spond-integration.md` rewritten end-to-end; privacy section documents the at-rest encryption + non-appearance in phone-home payloads. Closes #0062. | feat | v3.69.0 | ~12-18h estimated | ~3h actual |
| 0069 | HoD + tiles + docs polish bundle — six issues from a single round of acceptance testing collapsed into one PR. (1) **Wizard Cancel** stops triggering native required-field validation: `FrontendWizardView` adds `formnovalidate` to the Cancel button so it discards regardless of unfilled required fields (Save-as-draft and Back already had it; Cancel was the outlier). (2) **PDP tile group** split out of `Performance` into a new `Development` group; `pdp` and `pdp-planning` move there in `CoreSurfaceRegistration`. (3) **HoD academy-wide bypass**: `AuthorizationService::userHasPermission` adds a `tt_head_dev` shortcut after the existing administrator shortcut so HoD has academy-wide access by role definition without needing matrix scope-row plumbing tied to a `tt_people` record (root cause of "I'm HoD with all-green matrix and still see no players"). (4) **`hide_for_personas` tile flag**: new tile-shape field gated through `TileRegistry::tileVisibleFor()` via a new `personasFor()` private helper that delegates to `PersonaResolver::personasFor()`; applied to `functional-roles`, `configuration`, `migrations` so the HoD persona doesn't see admin-only surfaces (intentionally lighter than extending the matrix to gate presentation). (5) **Per-persona dashboard toggle**: `PersonaLandingRenderer::shouldRender()` now checks `persona_dashboard.{persona}.enabled` per-persona override first, falls back to the global flag; `FrontendConfigurationView::renderDashboardForm()` adds a per-persona table after the global radios with 9 personas and select options Inherit / Persona dashboard / Classic; `ConfigRestController::ALLOWED_KEYS` whitelists 9 new dotted keys. (6) **Docs internal-link rewrite + orphan cleanup**: `Markdown::inline()` link callback adds a `<slug>.md` (and `<locale>/<slug>.md`) regex branch that rewrites cross-references to `?tt_view=docs&topic=<slug>` so internal links stay inside the in-product docs viewer instead of silently rendering as plain text; `HelpTopics::all()` registers 6 previously-orphan docs (`custom-css`, `demo-data-excel`, `pdp-planning`, `player-status`, `spond-integration`, `staff-development`); dev-only docs intentionally left unregistered. ~6 new NL strings. Closes #0069. | feat | v3.68.0 | ~3-5h estimated | ~2h actual |
| 0067 | Q2 polish epic — shared patterns + detail pages + in-product comms across the admin tables and frontend dashboard. Foundations: new `RecordLink` helper + global `.tt-record-link` CSS, `ParentSearchPickerComponent` reusing the `.tt-psp` JS hydrator, framework-level Wizard Back button (history-stack-driven so conditional branches round-trip correctly), status-pill convergence — migration 0049 re-colours `activity_status.planned` to yellow, backfills `goal_status` colours, seeds `pdp_status` as a lookup type so PDP + Goals + Activities all render through one `LookupPill::render`. Detail pages: `FrontendPlayerDetailView` / `FrontendTeamDetailView` (staff via `tt_team_people` not the legacy `head_coach_id` column, roster table with `PlayerStatusCalculator` traffic-light dot per row) / `FrontendPersonDetailView`. Apply pass: every admin list (`Players` / `Teams` / `People` / `Evaluations` / `Activities` / `Goals` / `PDP` / `Podium`) linkified through the new helper; `EvaluationsPage` gets a rich filter bar (search / type / date range); `PlayerComparisonPage` swaps the four hardcoded native dropdowns for `PlayerSearchPicker` per slot with a `+` button up to 4; `PlayerRateCardsPage` swaps to `PlayerSearchPicker` + `required` validation; player edit form swaps the three legacy guardian text fields for `ParentSearchPicker` with migration 0050 adding `tt_players.parent_person_id`; new-person wizard registered under `Modules\Wizards\Person\` with three steps and `?role_hint` / `?return_to` round-trip support; `LabelTranslator` gets `roleType` + `personStatus`. PDP refactor: tab strip (Conversation / Evidence) replaces the cramped 1fr/280px sidebar layout; print page Close button replaced with a working server-side back URL; planning back-to-list button surfaces when arriving via a cell drill-down. Player-status overhaul: `>100%` weight sum returns inline error (no auto-redistribute); threshold display becomes a 14px gradient strip showing all three colour bands; new `FrontendPlayerStatusCaptureView` consolidates behaviour + potential capture into one form reachable from the player detail. In-product mail composer (`FrontendMailComposeView` + new `tt_send_email` cap via `RolesService::COMMS_CAPS`) — sends via `wp_mail`, every send writes a `mail_sent` audit row. Demo Excel: `_README` sheet leads the workbook with how-to-fill prose + auto_key explanation, Players sheet widens from 8 to 17 columns matching the admin form, Teams + People sheets get level/head_coach_key + phone/status. Demo Data admin page moves from `Tools` → `TalentTrack` parent. Frontend reports parity: `FrontendReportsLauncherView` + Analytics tile so the dashboard discovers the three wp-admin reports. Sprint 4 admin polish: WP-admin sidebar drops the duplicate Dashboard mirror via `Menu::renameDashboardMirror` at admin_menu priority 999; `HelpLink` opens in a new tab; `BackupSettingsPage` reads `wp_next_scheduled` for actual next-run; `StaffOverviewView` filter scopes top-down review to staff role types only; goal-conversation header gets a `How does this work?` link to docs/conversational-goals.md; migration 0051 seeds the `age_group` lookup if empty; NewTeam wizard adds a Roster step (`tt_players.team_id` bulk update on save); trial-details step grey-out via `WizardStepInterface::notApplicableFor` opt-in; `ModulesPage` redo as a grouped accordion (Core / People & teams / Performance / Analytics / Configuration / Optional) with one-line description per module. ~30 new NL strings. Closes #0067; defers team chemistry rebuild to #0068 (parked idea). | epic | _next_ | ~55-67h estimated | _TBD_ |
| v3.65.1-bundle (#0065 hotfix) | Admin Center receiver URL hotfix. v3.65.0 shipped `Sender::DEFAULT_URL` set to `https://ops.talenttrack.app/wp-json/ttac/v1/ingest` — a placeholder lifted from an `e.g.` line in the spec rather than a confirmed hostname. Patched to the actual mothership at `https://www.mediamaniacs.nl/wp-json/ttac/v1/ingest`. Docs (EN + NL), CHANGES, and readme references updated; filter / constant override paths unchanged. | fix | v3.65.1 | n/a | ~10min actual |
| 0065 (TT-side) | Admin Center foundation — TalentTrack-side phone-home client (#0065). New module `src/Modules/AdminCenterClient/` with `InstallId` (UUID v4 in `wp_options:tt_install_id`), `PayloadBuilder` (locked v1 shape: counts + module status + license-tier-or-null + error class names from `tt_audit_log`, never per-player rows), `Signer` (canonical JSON: recursive `ksort` + no whitespace + UTF-8; HMAC-SHA256), `Sender` (fire-and-forget `wp_remote_post` with 10s timeout, silent on network/5xx, warn-once-per-24h on 4xx). Four trigger paths: `Cron/DailyCron` (daily wp-cron), `Hooks/ActivationHook` (single-shot 30s out via wp-cron so activation isn't slowed), `Hooks/DeactivationHook` (best-effort sync send wired via `register_deactivation_hook`), `Hooks/VersionChangeHook` (compares `TT_VERSION` to `wp_options:tt_last_phoned_version` on `init`, schedules a 5s-out single-shot when they differ). v1 HMAC secret derivation **locked at `hash('sha256', install_id . '|' . site_url)`** per the refined TTA-side spec (`talenttrack-admin-center/specs/0001-feat-foundation-monitoring.md`); license-key-derived secret deferred to billing-oversight. Endpoint `POST https://www.mediamaniacs.nl/wp-json/ttac/v1/ingest` (locked in v3.65.1 — v3.65.0 shipped with the placeholder `ops.talenttrack.app` from the spec example), header `X-TTAC-Signature: sha256=<hex>`, payload `protocol_version: "1.0"`, all filterable for dev/staging via `tt_admin_center_url` or the constant `TT_ADMIN_CENTER_URL`. License fields nullable (no Freemius dependency in v1) plus the `module_status` sub-keys nullable until each module ships. New CI job `admin-center-self-check` runs `bin/admin-center-self-check.php` on every PR — three assertions (shape vs `tests/fixtures/admin-center-payload.schema.php`, privacy walk for forbidden field names, sign round-trip with order-stable canonicalisation); stubs the WP API surface so it executes on a vanilla PHP runner. EN + NL `docs/phone-home.md` covers every payload field, the privacy boundary, and the operational-telemetry posture. Closes the TT-side half of #0065; mothership-side continues in the `talenttrack-admin-center` repo. | feat | v3.65.0 | ~13h estimated / shipped in one bundle | ~3h actual |
| 0064 | Custom CSS independence (#0064) — club-admin styling surface that lets TalentTrack look exactly the way the club wants regardless of which WordPress theme is active. Companion to the Branding page (#0023), which goes the other way (defer to the active theme). One bundled PR shipping foundation + all three authoring paths + wp-admin coverage + history + revert. New page at `?tt_view=custom-css` (cap-gated on the new `tt_admin_styling` cap, granted to Administrator + Club Admin). Surface switcher toggles between **Frontend dashboard** and **wp-admin pages**; each surface has its own enabled toggle + CSS payload. Four tabs: **Visual settings** (Path C — 21 fields mapped to `--tt-*` CSS custom properties on `.tt-root`: colours, fonts, weights, corner radii, spacing scale, shadow strength), **CSS editor** (Path B — `wp_enqueue_code_editor` / CodeMirror with syntax highlighting + line numbers), **Upload + templates** (Path A — `.css` file upload + 3 light-leaning starter templates: Fresh light / Classic football / Minimal), **History** (last 10 auto-saves + named presets, **Revert** restores prior CSS and creates a fresh history row). Safety rails: scoped class isolation via `tt-root` body class on every TT surface (frontend `body_class` + admin `admin_body_class`); block-list sanitizer on save (`url(javascript:…)`, `url(data:text/html…)`, `expression()`, `behavior:`, `-moz-binding`, remote `@import`, external `@font-face` URLs all rejected with inline error pointing at the offending fragment); 200 KB hard cap; mobile-first guarantee (Path C exposes no layout-affecting controls); mutex with #0023 theme inheritance (turning Frontend Custom CSS on auto-disables Theme inheritance); safe mode at `?tt_safe_css=1` skips custom CSS for that pageview. Storage: live payload in `tt_config` keyed `custom_css.<surface>.css` / `.enabled` / `.version` / `.visual_settings`; new migration 0049 adds `tt_custom_css_history` for rolling auto-saves + named presets, scoped to `club_id` per the SaaS-readiness baseline. New module `src/Modules/CustomCss/` with `CustomCssRepository` / `CssSanitizer` / `StarterTemplates` / `VisualEditor` (Path C generator) / `CustomCssEnqueue` (frontend + admin inline emit + safe-mode) / `Frontend/FrontendCustomCssView` (single-page UI). Wired into `config/modules.php`, `DashboardShortcode` admin slugs/dispatch, and `FrontendConfigurationView` admin tile grid. ~75 new NL `.po` strings + EN/NL docs (`docs/custom-css.md` + `docs/nl_NL/custom-css.md`). Closes #0064. | feat | v3.64.0 | ~100-140h estimated / shipped in one bundle | ~5h actual |
| v3.63.0-bundle (#0061 round 3 — me-group + theme isolation) | Companion bundle to v3.62.0's gap follow-up. Eight-item player-surface rework: (1) Merged My profile into My card via new `FrontendMyProfileView::renderSections()` helper; account section removed. (2) New `FrontendMySettingsView` at `?tt_view=my-settings` — TT-rendered name/email/password using `wp_update_user`/`wp_set_password`; replaces the wp-admin/profile.php link in the user dropdown. (3) My team rework: podium first + #N of M rank badge on the viewer's own card via new `TeamStatsService::getRankInTeam()`; protects other teammates' ranks. (4) My journey restyled as a true vertical timeline via new `assets/css/frontend-journey.css`; markup classes tightened. (5) My evaluations row shrunk ~30% (badge column 110→80px, padding tightened). (6) Theme-isolation block in `frontend-admin.css` scoped under `body:not(.tt-theme-inherit) .tt-dashboard …` — line-height, letter-spacing, placeholder colour, z-index baseline 99999, `.tt-field` margin-bottom assertion. (7) Goal-list cards on My goals get a "Open conversation (N messages)" CTA via `ThreadMessagesRepository::listForThread`. (8) `docs/player-dashboard.md` + Dutch counterpart rewritten for the merged layout; ~30 new NL strings. Migration 0046 numbered renamed to 0048 elsewhere is unaffected. | feat | v3.63.0 | ~6-9h estimated | ~3h actual |
| v3.62.0-bundle (#0061 round 3 — gap follow-up) | The remainder of the user's punch-list that round 1 + 2 didn't cover. Editor polish: KPI palette categories default-collapsed, "Added X to canvas" toast, mobile-preview overflow CSS override (cards force `grid-column: auto` when previewing 360px), drag visual feedback (`is-dragging` class + drop-target highlight + `body.tt-pde-dragging` cursor), resize-clamp so M→S/L→S resizing keeps the slot inside the 12-col grid, new toolbar **Library** toggle that hides the palette pane and lets the canvas reclaim 18rem. PDP planning view + tile re-gated on `tt_edit_pdp` (HoD/coach/admin) so the cap-as-contract chokepoint denies the player persona that legitimately holds `tt_view_pdp` for self-scope. `FrontendMyActivitiesView` query gets defensive `GROUP BY a.id` against the legacy guest→player promotion duplicates. Frontend Configuration parity: new combined **Lookups** tile drilling into a sub-list of all 10 lookup categories (eval types / activity types / game subtypes / positions / foot / age groups / goal statuses / goal priorities / attendance statuses / rating scale). New **Spond admin overview** page at `Configuration → Spond` (admin sidebar + frontend tile) listing every team + Spond URL state + last sync at + status + per-row "Refresh now" button. New per-record detail views: `?tt_view=my-goals&id=N` shows the full goal + thread inline; `?tt_view=my-activities&id=N` shows the activity card; both with `FrontendBackButton`. Profile rows for goals + upcoming activities now wrap in those detail links. Journey Reset button gets `Wissen` Dutch translation. ~25 new NL `.po` strings. | feat | v3.62.0 | ~10-14h estimated | _TBD_ |
| v3.61.0-bundle (#0061 round 2) | Closes the deferred half of #0061. New-activity wizard (4 steps: Team → Type+Status → Details → Review) registered in `WizardsModule`; frontend "+ New activity" button now routed via `WizardEntryPoint::urlFor()`. New `SupportsCancelAsDraft` framework interface — wizards opt-in to render a "Save as draft" button; the new-activity wizard implements it by writing a `draft`-status row (the status seeded in v3.59.0 with `meta.hidden_from_form = 1`) so in-progress work can be resumed from the activities list. Authorization Matrix rendering reorganised into category headers (Players / Teams / Activities / Evaluations / Development / Insights / Operations / Administration) instead of one flat alphabetic list — pure rendering change, no DB migration. | feat | v3.61.0 | ~3-5h estimated / ~1.5h compressed | _TBD_ |
| 0039 | Staff development module — six new tables (migration 0048: `tt_staff_goals`, `tt_staff_evaluations`, `tt_staff_eval_ratings`, `tt_staff_certifications`, `tt_staff_pdp`, `tt_staff_mentorships`) + `tt_eval_categories.is_staff_tree` flag; seeded `cert_type` lookup (6 standard rows) + 5 staff eval-category roots; new Mentor functional role; three caps (`tt_view_staff_development` / `tt_manage_staff_development` / `tt_view_staff_certifications_expiry`); consolidated REST controller covering all five resources; five frontend views (`my-staff-pdp`, `my-staff-goals`, `my-staff-evaluations`, `my-staff-certifications`, `staff-overview`); four workflow templates (annual self-eval / top-down review / certification expiring / PDP season review) using a shared `StaffStubForm` placeholder; new "Staff development" tile group; module registered between Trials and Wizards. ~50 NL strings + EN/NL docs. | feat | v3.60.0 | ~30-38h / ~4-6h compressed | ~3h actual |
| v3.59.0-bundle (#0061 round 1) | Polish + bug bundle. Bugs: attendance % was form-completeness instead of presence-rate; activity / game-subtype / status dropdowns showed English entries (migration 0046 backfills missing `translations.nl_NL.name`); new-evaluation wizard's eval-type dropdown was empty (filtered on non-existent `archived_at`); delete activity used native `confirm()`. Polish: status as colour pill on both lists, attendance section hides unless status=completed, `draft` activity_status added (migration 0047, hidden from form). Features: new-evaluation wizard step 1 uses `PlayerSearchPickerComponent`, persona/classic dashboard chooser reachable from wp-admin via Configuration-landing notice. New idea `ideas/0061-feat-minor-polish-bundle.md` captures the deferred items (new-activity wizard + auth-matrix tile coverage). | feat | v3.59.0 | ~6-10h estimated / ~3-5h compressed | _TBD_ |
| 0042 | Youth-aware contact strategy — `audience:player` + `audience:parent` doc markers, `AgeTier` resolver (u8_u10 / u11_u12 / u12_plus / unknown), encrypted `tt_phone` user-meta via new `Infrastructure\Identity\PhoneMeta` (sha256 hash + verified-at), profile field on the WP user editor + AgeTier banner on the player edit form, migration 0046 `tt_push_subscriptions` (one row per device), `Push` module: `VapidKeyManager` (P-256 keypair generated once, encrypted at rest), in-house `WebPushSender` (RFC 8291 aes128gcm + RFC 8292 ES256 JWT, no Composer dep), `PushDispatcher` + `EmailDispatcher` + `ParentEmailDispatcher` + `DispatcherChain` runner, daily 90-day prune cron + HTTP-410 endpoint cleanup, REST `POST/GET/DELETE /push-subscriptions`, service worker `assets/js/tt-sw.js` + client glue `assets/js/tt-push-client.js`, install banner on dashboard for player+parent personas, 4 KB articles in EN+NL (install-on-iphone, install-on-android, notifications-setup, parent-handles-everything) + new `mobile` HelpTopics group, migration 0047 adds `dispatcher_chain` column to `tt_workflow_template_config` + admin select on the Workflow templates surface, `TaskMailer` consults the chain for non-default templates. Closes #0042. | feat | v3.58.0 | ~24-36h estimated | _TBD_ |
| 0056 (Sprint D) | Mobile-first activities pilot — new `assets/css/frontend-activities-manage.css` partial owns the responsive layout for the activity form + attendance table, authored mobile-first per `CLAUDE.md` § 2 (base 360px stacked cards → 768px row table → 1024px tightened padding). `FrontendActivitiesManageView::enqueueAssets()` overrides the parent to enqueue it. Removed the `@media (max-width: 639px)` block for `.tt-attendance-table` from `frontend-admin.css`. New `docs/architecture-mobile-first.md` (EN + NL) documents the migration recipe so the next view (Goals / Players / Trials / PDP) can follow. `CLAUDE.md` § 2 tightened: legacy migration anchored to one-view-per-release cadence, `inputmode` rule promoted to "treat as bug", `.tt-form-row` font-size note closed (resolved in v3.50.0). Closes #0056. | feat | v3.57.0 | ~5-7h estimated | ~1.5h actual |
| v3.56.0-bundle | #0059 Excel-driven demo data finished — `SheetSchemas` extended 2 → 15 sheets (Master / Transactional / Configuration / Reference, tab-coloured), `TemplateBuilder` streams .xlsx fresh on every download, `ExcelImporter` v1.5 covers Teams / People / Players / Trial_Cases / Sessions / Session_Attendance / Evaluations / Evaluation_Ratings / Goals / Player_Journey with cross-sheet FK validation, `DemoGenerator::run()` accepts a `source` option (procedural / excel / hybrid) routing through the importer + skipping procedural generators for sheets the workbook covered, demo admin page leads with a Step 0 — Source picker. Plus #0052 PR-B follow-up: `club_id` scope on `LookupsRestController` (5 sites) + `AuditLogRestController::list`. | feat | v3.56.0 | ~6-10h estimated | _TBD_ |
| 0052 (PR-B + PR-C) | SaaS-readiness baseline (closing). PR-B: 3 new REST controllers (`lookups`, `audit-log`, `invitations`) under `talenttrack/v1`, port-on-touch policy documented in `contributing.md` + backlog at `dev-tier-rest-port-backlog.md`, `RoleResolver` helper localising the 3 legitimate role-aware reads (Onboarding, DemoDataCleaner, AudienceResolver), 3 over-broad `is_user_logged_in()` REST gates tightened to capability checks (Evaluations list+get, Players list). PR-C: 3 `uploads/` refs audited (Backup module's `LocalDestination` is the intended FS-write behind `BackupDestinationInterface`; documented in `architecture.md` § Storage), 5 `wp_cron` sites categorised (4 infra stay, 1 domain — `TrialReminderScheduler` — flagged port-on-touch in `architecture.md` § Background work), hand-written `docs/openapi.yaml` covering every v1 path, `bin/contract-test.php` envelope validator, v1→v2 migration policy codified in `rest-api.md`. Closes the #0052 epic | feat | _next_ | ~18-26h estimated / ~6-10h compressed | _TBD_ |
| v3.54.0-bundle | Default-dashboard chooser surfaced in the frontend Configuration view as a "Default dashboard" sub-tile (radio: persona dashboard / classic tile grid). Whitelists `persona_dashboard.enabled` in `ConfigRestController::ALLOWED_KEYS` and stops `sanitize_key()` from stripping the dot in dotted config keys. UI-only entry point for the flag the v3.51.0 sprint-3 flip turned on by default. | feat | v3.54.0 | ~30min estimated | ~30min actual |
| v3.53.0-bundle | #0054 PDP planning windows + HoD `?tt_view=pdp-planning` matrix dashboard (migration 0043, `pdp_planning_window_days` config); #0057 Sprint 3 methodology config admin UI at `?tt_view=player-status-methodology` (migration 0044, per-age-group input weights + thresholds + behaviour-floor); #0057 Sprint 5 PDP integration (migration 0045 adds 3 cols to `tt_pdp_verdicts`, new `EvidencePacket` aggregator, REST `GET /pdp-files/{id}/evidence-packet`, verdict upsert captures system status + rejects with 400 when human disagrees and `divergence_notes` is empty); #0059 Excel-driven demo data for Teams + Players (composer adds `phpoffice/phpspreadsheet`, release.yml bundles production vendor, new `Excel\SheetSchemas` + `ExcelImporter` + `TemplateBuilder`, admin-page upload+import). Plus `club_id` scope side fix in `SystemHealthStripWidget`. | feat | v3.53.0 | ~22-31h estimated / ~7-12h compressed | _TBD_ |
| 0028 | Goals as a conversation — polymorphic `tt_thread_messages` + `tt_thread_reads` primitive (migration 0043), `ThreadTypeRegistry` + `ThreadTypeAdapter` interface, `GoalThreadAdapter`, `ThreadMessagesRepository` + `ThreadReadsRepository`, REST (list/post/edit/delete/read), `FrontendThreadView` chat-style component (mobile-first, 30s polling, edit window 5min, soft-delete, ARIA polite live region, focus-visible, reduced-motion), goal-detail integration in `FrontendGoalsManageView`, system messages on goal create + status change, `tt_thread_message_*` audit subscriber, email fan-out to participants minus author (private_to_coach gates to coaches+admins) with `threads.notify_on_post` config kill-switch. v1 wires only the `goal` thread type; #0017 / #0014 / #0044 follow-ups will register their own | feat | _next_ | ~20-25h | _TBD_ |
| v3.51.1-hotfix | Drop `session_id` legacy column probe in `AttendancePctRolling`; the no-legacy CI gate from #0035 forbids `\bsession_id\b` even in defensive fallbacks. Replaced with a single `activity_id` probe; installs that haven't run migration 0027 cleanly drop to `KpiValue::unavailable()`. | bug | v3.51.1 | ~5min | ~5min |
| 0060 | Persona dashboard authoring platform — 3-sprint epic complete. Sprint 1 (PR #118): foundation + 14-widget catalog + 25-KPI catalog + 8 defaults + REST + role-switcher. Sprint 2 (PR #119): drag-drop editor at `Configuration → Dashboard layouts` with draft/publish/reset, undo/redo, keyboard a11y, preview-as-persona, mobile preview, audit-log. Sprint 3 (PR #121): hero data wired (rate card, today/up-next, child-switcher recap, system health, assigned players), KPI sparklines for evaluations + attendance, coach team tabs, Configuration tile entry, flag flipped on by default. | epic | v3.51.0 | ~70-95h estimated / ~23-33h compressed | _TBD_ |
| v3.50.1-hotfix | Replace legacy `tt_edit_sessions` cap reference in `ActionCardWidget` with `tt_edit_activities` (post-#0035 rename). CI no-legacy gate had been blocking the `talenttrack.zip` build since #0060 sprint 1. | bug | v3.50.1 | ~5min | ~5min |
| v3.50.0-bundle | Wizard-first standard codified in `CLAUDE.md` § 3 + spec template + DoD (#0058); read-only Spond iCal sync per team with new `CredentialEncryption` helper, hourly cron, REST refresh, soft-archive on UID removal (#0031); player-status core — behaviour + potential schema + `PlayerStatusCalculator` + `MethodologyResolver` + REST + traffic-light dot on team-players panel (#0057 Sprints 1+2+4-min, Sprints 3+5 deferred to v3.51.0); mobile-first quick wins — legacy form font-size bump, `inputmode` on `CustomFieldRenderer`, `touch-action: manipulation`, `:focus-visible`, safe-area insets, 48px tap-target floor under `(pointer: coarse)`, new `desktop_preferred` `TileRegistry` flag with non-blocking phone banner on 6 admin surfaces (#0056 quick wins; pilot rewrite deferred). #0059 Excel-driven demo data deferred to v3.51.0+ | feat | v3.50.0 | ~58-83h estimated / ~13-20h compressed | _TBD_ |
| v3.49.0-bundle | Trial inline-create flow (player picker + new-player disclosure on the trial-case create form), `StaffPickerComponent` (autocomplete staff picker reusing the `.tt-psp` hydrator, replaces 3 plain selects: trial assign, trial create, new-team wizard), Configuration tile sub-page (sub-tile grid mirroring wp-admin Configuration submenu) | feat | v3.49.0 | ~3-5h estimated | ~1.5h actual |
| v3.48.0-bundle | Demo-readiness round 2: License-module-disabled honoured by tier gates (was always firing), parents see the Me-group dashboard tiles (was empty), workflow cadence/deadline labels rewritten with help tooltips, journey filter bar collapsed (3 primary + "More filters (N)" toggle), trial cases create form gets a 2-column desktop layout, "Roles & Permissions" admin menu renamed to "Roles & rights" with cross-links to Functional roles | bug | v3.48.0 | ~3-5h estimated | ~1.5h actual |
| v3.47.0-bundle | Activity status + source columns (lookup-driven), tournament + meeting added to type seed + meta.color backfill, admin filter dropdown moved from hardcoded to lookup-driven, colour-coded type pill in admin + frontend activity lists, wizard config UX rebuild (text-input → checkbox grid + master toggle), cohort tile fatal fix (array-vs-object access + cohortByType param-order), club_id sweep on ActivitiesPage admin queries (gap missed by v3.45.1 audit regex) | feat | v3.47.0 | ~3-5h estimated | ~2h actual |
| v3.46.0-hotfix | Demo-readiness hotfix bundle — methodology tile leak to player+parent removed, wizards work on subdirectory installs + Cancel button fixed, eval rating-detail toggle works (CSS specificity fix), club admin gets `tt_access_frontend_admin`, observer no longer granted `tt_submit_idea`, trial tile icons (track + categories), My journey tile reordered first, My sessions → My activities | bug | v3.46.0 | ~3-5h estimated | ~1h actual |
| 0052 (PR-A) | SaaS-readiness baseline (PR-A) — tenancy scaffold + tt_config reshape (~50 tables get club_id, 5 root entities get uuid, composite (club_id, config_key) PK, CurrentClub resolver, 8 call sites refactored, audit script). Unblocks PR-B + PR-C. | feat | v3.45.0 | ~12-18h | ~5h |
| 0052 (PR-A sweep) | SaaS-readiness baseline (PR-A follow-up) — repository sweep (114 files: every read filters on `club_id`, every write populates `club_id`; new `bin/audit-tenancy-source.sh` CI guardrail). Closes the "Repository sweep deferred" caveat. | feat | v3.45.1 | ~7-13h (PR-A budget remainder) | _TBD_ |
| 0053 | Player journey — chronological events spine + injuries + cohort transitions (2 new tables, 14 event types, 2 new caps, REST + 3 dispatcher slugs + injury_recovery_due workflow) | epic | v3.44.0 | ~4-5 sprints / ~6-9h compressed | ~6h |
| 0055 | Record-creation wizards (framework + 4 wizards: player/team/eval/goal + admin analytics) | epic | v3.43.0 | ~32-46h | ~3.5h |
| 0017 | Trial player module (case workflow + 3 letter templates + parent-meeting view + editors) | epic | v3.42.0 | ~59-72h | ~5h |
| pdp-quality-pass | PDP quality pass + coach/team auth bug fix (legacy + role-scope merge, PDP UX polish, NL POP rename) | feat | v3.41.0 | ~6-8h | ~3h |
| 0014 (sprints 3-5) | Report generator: configurable renderer + 4-step audience wizard + scout flow | epic | v3.40.0 | ~40-48h | _TBD_ |
| 0033-trustworthy-matrix | Authorization matrix scope-aware bridge + complete persona seed + entity-name alignment | feat | v3.39.0 | ~5-8h | ~2.5h |
| 0014 (sprint 2) | My profile rebuild — six-section player dashboard with embedded FIFA card | feat | v3.38.0 | ~10-12h | _TBD_ |
| 0022 (Phase 2+3) | Workflow engine — chain primitive + snooze + bulk + event-log retry | epic | v3.37.0 | ~65-85h | ~5h — 14x compression |
| 0033-finalisation-fix | Hotfix: tile slug-fallback restored after registry migration | bug | v3.36.1 | ~30min | ~20min |
| 0033-finalisation | Tile + admin-menu registry migration (declarative TileRegistry + AdminMenuRegistry) | feat | v3.36.0 | ~6-10h | ~2.5h |
| 0051 | Module surface gating — disabled modules disappear from tiles + menus + URLs | feat | v3.35.0 | ~4-6h | ~2h |
| 0050 | Activity Type lookup-driven with per-type workflow policy + HoD rollup | feat | v3.33.0 | ~10h | ~5-7h |
| 0018 | Team development + chemistry (CompatibilityEngine + formation board + pairings) | epic | v3.32.0 / v3.34.0 | ~56-70h | ~9h — 7x compression |
| 0049 | Activity form Type/subtype/Other label fields + demo-mode guest tag fix | bug | v3.31.1 | ~1h | ~45min |
| 0048 | User docs cleanup — audience comment leak fix + 13 EN + 12 NL doc rewrites | feat | v3.30.1 | ~2h | ~1.5h |
| 0044 | PDP cycle — per-season conversations + verdict + workflow cadence + carryover | epic | v3.30.0 / v3.31.0 | ~30-42h | ~8h — 5x compression |
| 0047 | Dashboard regroup + Configuration tile-landing + i18n cleanup | feat | v3.29.0 | ~11h | ~5-7h |
| 0046 | Hotfix: guest-add modal popping up on page load (CSS specificity) | bug | v3.28.2 | ~10min | ~5min |
| 0045 | Hotfix: frontend dashboard fatal — undefined `shortcodeBaseUrl()` | bug | v3.28.1 | ~15min | ~10min |
| 0041 | Demo-readiness follow-up (App KPIs rename + help drawer + my-sessions filter + linked-parent) | epic | v3.28.0 | ~6-8h | ~3h |
| 0040 | Demo-readiness omnibus — 16 polish items in one PR | epic | v3.27.0 | ~14-20h | ~5h |
| 0012 | Anti-AI fingerprint pass — Unicode banners stripped + version-history docblocks trimmed | epic | v3.26.1 | ~50-75h | ~1.5h first pass |
| 0033 | Authorization matrix + module toggles + per-module config + dashboard tile split (9-sprint epic) | epic | v3.24.0–v3.26.0 | ~90h | ~14h — 6.4x compression |
| 0021 | Audit log viewer + bundled audit-log schema-drift fix | feat | v3.25.0 | ~10h | ~2h |
| 0038 | Hotfix: fresh install had no usable surface (Activator auto-page + Menu parent=null) | bug | v3.24.2 | ~1-2h | ~45min |
| 0037 | Guest-attendance fatal fix + UX polish (button on create + fuzzy team-filter picker) | bug | v3.24.1 | ~1-2h | ~1.5h |
| 0036 | Dashboard UI polish — smaller tiles + scale + modern icons + demo pill + help icon + logo | feat | v3.24.0 | ~2-3h | ~2h |
| 0035 | Sessions → activities rename + typed activities (game/training/other) + no-legacy CI gate | feat | v3.24.0 | ~18-22h | ~8h |
| 0032 | User invitation flow (submit/accept/share via WhatsApp + dedicated `tt_parent` role) | feat | v3.24.0 | ~14-20h | ~8h |
| 0030 | Branding + go-to-market site (separate plugin) | epic | external — [talenttrack-branding](https://github.com/caspernieuwenhuizen/talenttrack-branding) | ~45-65h | ~3h scaffold |
| 0025 | Multilingual auto-translate (opt-in, default OFF) | feat | v3.23.0 | ~26h | ~12h |
| 0009 | Development management (submit → refine → approve → promote-to-GitHub) | epic | v3.22.0 | ~30h | ~6h |
| 0022 (Phase 1) | Workflow engine — engine + 5 templates + inbox + dashboard + admin config | epic | v3.22.0 | ~62h | ~14h |
| 0029 | Documentation split (audience markers + role-filtered TOC + dev-tier docs) | feat | v3.22.0 | ~17h | ~6h |
| 0034 | Custom icon system — replace dashicons + emoji | feat | v3.22.0 | — | ~5h |
| 0026 | Guest-player attendance | feat | v3.22.0 | ~10h | ~10h |
| 0027 | Football methodology module (framework + PDF content + visuals + per-club primer) | feat | v3.19.0 / v3.21.0 | ~52h + expansion | ~32h + ~50h |
| 0003 | Player evaluations view polish | feat | v3.18.0 | ~6h | ~3h |
| 0004 | My card tile polish | feat | v3.18.0 | ~5h | ~2h |
| 0011 | Monetization (licensing + tiers + caps + trial) | epic | v3.17.0 | ~44-55h | ~30h |
| 0013 | Backup + disaster recovery | epic | v3.15.0–v3.16.0 | ~50-63h | ~50h |
| 0024 | Setup wizard / onboarding | feat | v3.14.0 | ~10-30h | ~10-12h |
| **0019** | **Frontend-first admin migration** | **epic** | v3.7.0–v3.12.0 | ~120-150h | **~73h** |
| 0023 | Styling options + theme inheritance | feat | v3.8.0 | ~8h | ~6h |
| 0020 | Demo data generator | feat | v3.2.0–v3.6.1 | ~24h | ~30h |
| 0008 | Actions Node 20 deprecation | bug | v3.6.1 | ~4h | 5 min |
| 0007 | Lookup drag-reorder bug | bug | v3.6.1 | ~TBD | 15 min |
| 0001 | Docs language support | feat | v3.1.0 | — | — |
| 0002 | Roles overview / reference | feat | absorbed into #0019 Sprint 5 | — | — |
| 0005 | Frontend full CRUD | epic | superseded by #0019 | — | — |

## Skipped

| # | Topic | Reason |
| - | - | - |
| 0015 | FrontendMyProfileView fatal | Not reproducible (spec stale) |

## What's next

v3.22.0 closed five items in a single release: #0026 (~10h), #0022 Phase 1 (~14h), #0029 (~6h), #0009 (~6h), #0034 (~5h) — **~41h of work, vs ~119h estimate**. The estimation gap is mostly because:

1. Compressed sprints into single PRs (the #0011 / #0013 / #0027 pattern continues).
2. Inline-shaping with bolded recommendations resolved 8 open questions on #0009 in seconds.
3. Stacked PRs (#54 → #56 → #57) avoided the rebase-conflict cost most teams pay on stacks.

v3.23.0 lands #0025 standalone (~12h actual vs ~26h estimate). The translation layer is the scope-multiplier that downsizes #0010 (multi-language UI) — clubs can now opt in to auto-translation for user-entered text and #0010's manual-translation scope shrinks to just the bundled UI strings.

v3.24.0 bundled four items: #0032 invitation flow (~8h actual vs ~14-20h estimate), #0033 sprint 1 of 9 (authorization-matrix schema + read API), #0035 sessions→activities rename (~8h actual vs ~18-22h estimate, 73 files touched, no-legacy CI gate now active), and #0036 dashboard polish (~2h). Two new compression data points: #0032 came in within the *low* estimate, and #0035 — the largest single find/replace + module move yet attempted — came in **at half** of its low estimate. The CI grep gate on #0035 is the first guardrail against regression of a no-legacy rename; future rename-style changes can borrow the pattern.

v3.24.1 shipped #0037 (guest-attendance fatal + UX polish, ~1.5h actual vs ~1-2h estimate — on-target, not a compression data point but worth logging). Same-day after v3.24.1, #0033 advanced from sprint 1 to sprint 5 in two PRs (sprint 2 dormant legacy bridge, sprints 3+4+5 bundled into matrix admin UI + tile registry + dashboard split + module toggles, ~12h actual across the four sprints — extends the compression pattern into mid-epic sprint slices). v3.24.2 then shipped #0038 fresh-install fix (~45min actual vs ~1-2h estimate).

Realistic next moves:

1. **Pick from "Not started"** — best-leverage candidates:
   - **#0014 Player profile rebuild (~58h)** — unblocks #0017 (trial player module, ~72h after this).
   - **#0006 Team planning module (~55h)** — pairs well with #0022 (sessions are templated, scheduling on a calendar is the obvious next surface).
   - **#0010 Multi-language (FR/DE/ES) (~80-140h, likely lower)** — re-estimate now that #0025 + #0029 ship; the manual-translation surface is much smaller than originally scoped.
2. **#0028 shaped** — goals as conversational thread (~20-25h, polymorphic thread primitive + goal adapter); ready to build.

## Total backlog effort estimate

### Remaining work (post-v3.26.0)

| Bucket | Low | High |
| - | - | - |
| Ready (#0028) | 20 | 30 |
| Needs shaping (#0031, #0042, #0043) | 76 | 114 |
| Not started (#0006, #0010, #0014, #0016, #0017, #0018, #0022 Phase 2) | 435 | 505 |
| **Total remaining** | **~581h** | **~724h** |

Was ~700-870h at end of v3.21.0; v3.22.0 cleared ~119h of estimate (vs ~41h actual); v3.23.0 cleared another ~26h (vs ~12h actual); v3.24.0 cleared another ~32-42h estimate (vs ~16h actual); v3.24.1 + v3.24.2 + #0033 sprints 2-5 cleared another ~37-47h estimate (vs ~14h actual); v3.26.0 closed the #0033 epic with sprints 6-9 (~34h estimate vs ~5h actual). Post-v3.23.0 the picture also moved: #0033 promoted from "Needs shaping" to "Ready" with a locked ~90h estimate then ran the full epic (~90h estimate / ~14h actual / ~1/6.4 ratio); #0030 dropped out entirely (extracted to talenttrack-branding repo); #0042 (youth contact strategy) and #0043 (KB platform) were captured into "Needs shaping" on 27 April 2026, adding ~64-96h of estimate to the backlog. Remaining estimates intentionally conservative — empirical ~1/2.5 under-shoot rate across five releases suggests a realistic floor of **~350-435h** if the compression pattern holds.

### Lead time projection

At Casper's empirical pace (~6-8 effective driver-hours per evening on busy weeks; ~15-20h per week sustainable), and applying the iteration multiplier:

| Pace | Hours/week | Weeks remaining (low / high) | Months (low / high) |
| - | - | - | - |
| Optimistic — compression holds, 1.0× multiplier | 20 | 32 / 39 | ~7-9 |
| Realistic — 1.2× iteration multiplier | 15 | 51 / 63 | ~12-15 |
| Conservative — 1.5× multiplier on Casper-side authoring tasks (#0027 content, multi-language) | 12 | 79 / 98 | ~18-23 |

**Lead time floor: ~7-9 months** if the compression pattern holds (single-PR epics, stacked sprints, inline shaping, no-legacy renames with CI guardrails). **Realistic median: ~12-15 months** — mid-to-late-2027. The conservative upper bound (~23 months) reflects the reality that #0010 multi-language depends on Casper-side translation throughput, not engineering throughput. (#0030 was previously listed here too, but the marketing site has been extracted to its own repo and is now decoupled from this backlog's lead time.)

### Throughput calibration

Three releases of compounding data:

- **v3.22.0** — 41h actual vs 119h estimate → 1 / 2.9 multiplier (65% under-shoot).
- **v3.23.0** — 12h actual vs 26h estimate → 1 / 2.2 multiplier (54% under-shoot).
- **v3.24.0** — 16h actual vs 32-42h estimate → ~1 / 2.0-2.6 multiplier (50-62% under-shoot).
- **Cumulative across the three releases**: ~69h actual vs ~177-187h estimate → consistent ~1/2.5 ratio.
- **Post-v3.24.0 micro-work** (not standalone-release rows): v3.24.1 #0037 (~1.5h vs ~1-2h) and v3.24.2 #0038 (~45min vs ~1-2h) are point fixes hitting estimate; #0033 sprints 2-5 (~12h actual vs ~36-44h sprint-budget allocation) extend the compression pattern into mid-epic sprint slices.
- Driven by: stacking PRs, decision compression (inline shaping with bolded recommendations), single-PR epics, and the new no-legacy-rename pattern (#0035) where a CI grep gate catches regression risk so the rename can be aggressive without manual review of every callsite.
- Not all backlog items will compress this aggressively. Authoring-heavy work (#0010 translations to FR/DE/ES, #0027 PDF content authoring) is bound by Casper's own writing pace, not engineering velocity.
- Pure engineering items (#0014, #0021, #0006) are the most likely to repeat the compression.

## Principles

1. Don't widen scope prematurely. Single tier sold well beats three tiers with no traction (#0011).
2. Activation matters most. #0024 shipped first (v3.14.0); #0013 backup ships next; #0011 monetization after.
3. Track actual throughput. Three releases (v3.22.0, v3.23.0, v3.24.0) now show a consistent ~1/2.5 estimate-to-actual ratio — apply the discount carefully (it doesn't generalise to authoring tasks).
4. Deps are real, but they fall fast. #0017 was blocked behind #0022 Phase 1 + #0014 Sprint 3 — Phase 1 cleared in v3.22.0; #0014 is now the only remaining gate.
5. Compress sprints aggressively. Single-PR epics keep working: v3.22.0 had five-in-one, v3.24.0 had a 73-file no-legacy rename in one PR with a CI guardrail to prevent regression.
